# biz QA 轮次实现计划 — 2026-04-14

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 biz 业务系统跑一轮系统性 QA:修 creative pub/sub 契约缺口、建后端 Go e2e 测试基建与覆盖、脚本 harness 端到端跑业务流、前端 /qa 四维度 + DESIGN.md 合规扫描,发现 bug 就地修复,产出 QA 报告合回 main。

**Architecture:** 串行 5 Phase(P1 最小修复 → P2 e2e 基建+全覆盖 → P3 脚本 harness + bug triage → P4 前端 /qa + 契约漂移 → P5 /browse 截图 + 合并)。每 Phase 走 CLAUDE.md 的 verification 循环(requesting-code-review → verification-before-completion → /qa,整轮零问题才进下一 Phase,最多 5 轮)。

**Tech Stack:** Go 1.22+(`net/http` ServeMux, `pgx/v5`, `go-redis/v9`, `clickhouse-go/v2`)、Next.js(web)、Docker Compose(biz worktree 端口偏移 +11000)、bash + curl + jq + redis-cli + clickhouse-client(harness)、headless browser via /qa skill(前端)。

**关联文档**
- 设计 spec:`docs/superpowers/specs/2026-04-14-biz-qa-design.md`
- 契约:`docs/contracts/biz-engine.md`(本轮禁止修改,只读)
- 参考 commit:`becdc67`(campaign handler 的 pub/sub 发布模板)

**禁止事项(每个 task 都适用)**
- 不碰 `cmd/bidder`, `cmd/consumer`, `internal/bidder`(除了读 `NotifyCampaignUpdate` 的签名), `internal/events`
- 不改 `docs/contracts/biz-engine.md`
- 不改 `docker-compose.override.yml` / `.env`
- 不碰 `dsp.billing`, `SendLoss`

---

## 文件结构总览

**P1 创建 / 修改**
- Modify: `internal/campaign/store.go` — 新增 `GetCreativeByID`
- Modify: `internal/handler/campaign.go:383-493` — 3 个 creative handler 加 pub/sub
- Create: `internal/handler/e2e_creative_pubsub_test.go` — P1 的 3 条 pub/sub 测试(build tag `e2e`)

**P2 创建 / 修改**
- Create: `internal/handler/routes.go` — `BuildPublicMux` / `BuildAdminMux` / `BuildPublicHandler`
- Modify: `cmd/api/main.go:123-202` — 用 routes.go 的 builders 替换内联注册
- Create: `internal/handler/e2e_support_test.go` — helpers(`mustDeps`, `newAdvertiser`, `authedReq`, `adminReq`, `subscribeUpdates`, fixture)
- Modify: `internal/handler/e2e_creative_pubsub_test.go` — 迁移到 helpers
- Create: `internal/handler/e2e_public_advertiser_test.go`
- Create: `internal/handler/e2e_public_campaign_test.go`
- Create: `internal/handler/e2e_public_creative_test.go`
- Create: `internal/handler/e2e_public_report_test.go`
- Create: `internal/handler/e2e_public_meta_test.go`
- Create: `internal/handler/e2e_admin_registration_test.go`
- Create: `internal/handler/e2e_admin_creative_test.go`
- Create: `internal/handler/e2e_admin_system_test.go`
- Create: `internal/handler/e2e_authz_table_test.go`

**P3 创建**
- Create: `scripts/qa/run.sh` + 10 分步脚本 + `lib.sh` + `e2e-env.sh`
- Create: `docs/qa/2026-04-14-biz-qa-report.md`(初版)

**P4 修改**
- Modify: `web/app/**/*.tsx` / `globals.css`(按发现的 bug)
- Maybe modify: `docs/generated/openapi3.yaml`, `web/lib/api-types.ts`(如果发现阻塞漂移且有 regenerate 流程)
- Modify: `docs/qa/2026-04-14-biz-qa-report.md`(加前端章节)

**P5 创建 / 修改**
- Create: `docs/qa/screenshots/2026-04-14/*.png`(/browse 产出)
- Modify: `docs/qa/2026-04-14-biz-qa-report.md`(最终版 + 契约变更清单)

---

# Phase P1 · Creative pub/sub 修复

**目标**:POST/PUT/DELETE `/api/v1/creatives` 三条路由发 `campaign:updates` pub/sub,和 becdc67 的 campaign handler 模式一致。TDD:先写 3 条订阅测试(红)→ 改代码(绿)。

**Exit 标准**:`go test -tags=e2e -count=1 -run TestCreativePubSub ./internal/handler/...` 全绿;verification 循环零问题一轮;commit 可独立 revert。

---

### Task P1.1: 添加 `Store.GetCreativeByID`

**Files:**
- Modify: `internal/campaign/store.go`(在 `ListCreativesByStatus` 后,`UpdateCreative` 前)

**背景**:UPDATE / DELETE handler 要先查出 `campaignID` 再发 pub/sub,但 Store 目前只有 `GetCreativesByCampaign(campaignID)`,没有 `GetCreativeByID(id)`。新增这个最小方法。

- [ ] **Step 1: 在 store.go 里查现有 `ListCreativesByStatus` 的位置**

Run: `grep -n "ListCreativesByStatus" internal/campaign/store.go`
Expected: 匹配到 `378:func (s *Store) ListCreativesByStatus(...)`

- [ ] **Step 2: 在 `ListCreativesByStatus` 上方插入 `GetCreativeByID`**

在 `internal/campaign/store.go` 的 `ListCreativesByStatus` 定义之前插入:

```go
// GetCreativeByID returns a single creative by id (any status).
// Used by handlers that need CampaignID before UPDATE/DELETE.
func (s *Store) GetCreativeByID(ctx context.Context, id int64) (*Creative, error) {
	cr := &Creative{}
	err := s.db.QueryRow(ctx,
		`SELECT id, campaign_id, name, ad_type, format, size, ad_markup, destination_url, status,
		        COALESCE(native_title,''), COALESCE(native_desc,''), COALESCE(native_icon_url,''),
		        COALESCE(native_image_url,''), COALESCE(native_cta,''), created_at
		 FROM creatives WHERE id = $1`,
		id,
	).Scan(&cr.ID, &cr.CampaignID, &cr.Name, &cr.AdType, &cr.Format, &cr.Size,
		&cr.AdMarkup, &cr.DestinationURL, &cr.Status,
		&cr.NativeTitle, &cr.NativeDesc, &cr.NativeIconURL,
		&cr.NativeImageURL, &cr.NativeCTA, &cr.CreatedAt)
	if err != nil {
		return nil, err
	}
	return cr, nil
}
```

- [ ] **Step 3: 确认编译通过**

Run: `go build ./internal/campaign/...`
Expected: 无错误输出

- [ ] **Step 4: 运行现有 campaign 包单测确认没回归**

Run: `go test ./internal/campaign/...`
Expected: 全绿

- [ ] **Step 5: Commit**

```bash
git add internal/campaign/store.go
git commit -m "feat(campaign): add Store.GetCreativeByID for handler lookups

Needed so creative UPDATE/DELETE handlers can fetch campaign_id
before publishing campaign:updates.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P1.2: 写失败的 CreatePubSub 测试(TDD 红)

**Files:**
- Create: `internal/handler/e2e_creative_pubsub_test.go`

**背景**:P2 的 e2e 基建还没建,这里内联最小 fixture(连真实 pg/redis,env 对齐 biz worktree 端口偏移 +11000),P2 后会迁移到 helpers。

**前置条件**:`docker compose up -d` 已跑,pg/redis/ch 都 ready。

- [ ] **Step 1: 创建测试文件**

Create `internal/handler/e2e_creative_pubsub_test.go`:

```go
//go:build e2e
// +build e2e

package handler_test

import (
	"bytes"
	"context"
	"encoding/json"
	"fmt"
	"net/http"
	"net/http/httptest"
	"os"
	"testing"
	"time"

	"github.com/heartgryphon/dsp/internal/audit"
	"github.com/heartgryphon/dsp/internal/billing"
	"github.com/heartgryphon/dsp/internal/campaign"
	"github.com/heartgryphon/dsp/internal/handler"
	"github.com/heartgryphon/dsp/internal/registration"
	"github.com/jackc/pgx/v5/pgxpool"
	"github.com/redis/go-redis/v9"
)

// --- inline fixture (will be generalized in P2 into e2e_support_test.go) ---

func p1DSN() string {
	if v := os.Getenv("DSP_E2E_PG_DSN"); v != "" {
		return v
	}
	return "postgres://dsp:dsp@localhost:16432/dsp?sslmode=disable"
}

func p1RedisAddr() string {
	if v := os.Getenv("DSP_E2E_REDIS_ADDR"); v != "" {
		return v
	}
	return "localhost:17380"
}

func p1Deps(t *testing.T) *handler.Deps {
	t.Helper()
	ctx := context.Background()
	db, err := pgxpool.New(ctx, p1DSN())
	if err != nil {
		t.Skipf("postgres not reachable (%v) — run `docker compose up -d`", err)
	}
	if err := db.Ping(ctx); err != nil {
		t.Skipf("postgres ping failed (%v)", err)
	}
	rdb := redis.NewClient(&redis.Options{Addr: p1RedisAddr()})
	if err := rdb.Ping(ctx).Err(); err != nil {
		t.Skipf("redis not reachable (%v)", err)
	}
	t.Cleanup(func() {
		_ = rdb.Close()
		db.Close()
	})
	return &handler.Deps{
		Store:      campaign.NewStore(db),
		BillingSvc: billing.New(db),
		RegSvc:     registration.New(db),
		Redis:      rdb,
		AuditLog:   audit.NewLogger(db),
	}
}

func p1NewAdvertiser(t *testing.T, d *handler.Deps) int64 {
	t.Helper()
	ctx := context.Background()
	email := fmt.Sprintf("qa-%s-%d@test.local", t.Name(), time.Now().UnixNano())
	adv := &campaign.Advertiser{
		CompanyName:  "QA " + t.Name(),
		ContactEmail: email,
		APIKey:       handler.GenerateAPIKey(),
	}
	id, err := d.Store.CreateAdvertiser(ctx, adv)
	if err != nil {
		t.Fatalf("create advertiser: %v", err)
	}
	return id
}

func p1NewCampaign(t *testing.T, d *handler.Deps, advID int64) int64 {
	t.Helper()
	ctx := context.Background()
	c := &campaign.Campaign{
		AdvertiserID:  advID,
		Name:          "QA-" + t.Name(),
		BidCPMCents:   100,
		DailyBudget:   10000,
		Status:        "draft",
		BillingModel:  "cpm",
	}
	id, err := d.Store.CreateCampaign(ctx, c)
	if err != nil {
		t.Fatalf("create campaign: %v", err)
	}
	return id
}

func p1NewCreative(t *testing.T, d *handler.Deps, campaignID int64) int64 {
	t.Helper()
	ctx := context.Background()
	cr := &campaign.Creative{
		CampaignID:     campaignID,
		Name:           "QA-" + t.Name(),
		AdType:         "banner",
		Format:         "image",
		Size:           "300x250",
		AdMarkup:       "<a>ad</a>",
		DestinationURL: "https://example.com",
	}
	id, err := d.Store.CreateCreative(ctx, cr)
	if err != nil {
		t.Fatalf("create creative: %v", err)
	}
	return id
}

// p1WaitForUpdate subscribes to campaign:updates BEFORE returning.
// Returns wait(d) that reports whether a message with campaign_id was seen within d.
func p1WaitForUpdate(t *testing.T, rdb *redis.Client, campaignID int64) func(time.Duration) bool {
	t.Helper()
	ctx := context.Background()
	pubsub := rdb.Subscribe(ctx, "campaign:updates")
	// Block until subscription is confirmed.
	if _, err := pubsub.Receive(ctx); err != nil {
		t.Fatalf("subscribe confirm: %v", err)
	}
	got := make(chan struct{}, 1)
	go func() {
		for msg := range pubsub.Channel() {
			var payload struct {
				CampaignID int64  `json:"campaign_id"`
				Action     string `json:"action"`
			}
			if err := json.Unmarshal([]byte(msg.Payload), &payload); err != nil {
				continue
			}
			if payload.CampaignID == campaignID {
				select {
				case got <- struct{}{}:
				default:
				}
				return
			}
		}
	}()
	t.Cleanup(func() { _ = pubsub.Close() })
	return func(d time.Duration) bool {
		select {
		case <-got:
			return true
		case <-time.After(d):
			return false
		}
	}
}

// --- tests ---

func TestCreativePubSub_Create(t *testing.T) {
	d := p1Deps(t)
	advID := p1NewAdvertiser(t, d)
	campaignID := p1NewCampaign(t, d, advID)

	wait := p1WaitForUpdate(t, d.Redis, campaignID)

	body := map[string]any{
		"campaign_id":     campaignID,
		"name":            "c1",
		"ad_type":         "banner",
		"format":          "image",
		"size":            "300x250",
		"ad_markup":       "<a>ad</a>",
		"destination_url": "https://example.com",
	}
	buf, _ := json.Marshal(body)
	req := httptest.NewRequest("POST", "/api/v1/creatives", bytes.NewReader(buf))
	req.Header.Set("Content-Type", "application/json")
	w := httptest.NewRecorder()
	d.HandleCreateCreative(w, req)

	if w.Code != http.StatusCreated {
		t.Fatalf("want 201, got %d body=%s", w.Code, w.Body.String())
	}
	if !wait(2 * time.Second) {
		t.Fatalf("did not receive campaign:updates for campaign_id=%d within 2s", campaignID)
	}
}

func TestCreativePubSub_Update(t *testing.T) {
	d := p1Deps(t)
	advID := p1NewAdvertiser(t, d)
	campaignID := p1NewCampaign(t, d, advID)
	creativeID := p1NewCreative(t, d, campaignID)

	wait := p1WaitForUpdate(t, d.Redis, campaignID)

	body := map[string]any{
		"name":            "c1-updated",
		"ad_type":         "banner",
		"format":          "image",
		"size":            "300x250",
		"ad_markup":       "<a>new</a>",
		"destination_url": "https://example.com/new",
	}
	buf, _ := json.Marshal(body)
	req := httptest.NewRequest("PUT", fmt.Sprintf("/api/v1/creatives/%d", creativeID), bytes.NewReader(buf))
	req.SetPathValue("id", fmt.Sprintf("%d", creativeID))
	req.Header.Set("Content-Type", "application/json")
	w := httptest.NewRecorder()
	d.HandleUpdateCreative(w, req)

	if w.Code != http.StatusOK {
		t.Fatalf("want 200, got %d body=%s", w.Code, w.Body.String())
	}
	if !wait(2 * time.Second) {
		t.Fatalf("did not receive campaign:updates for campaign_id=%d within 2s", campaignID)
	}
}

func TestCreativePubSub_Delete(t *testing.T) {
	d := p1Deps(t)
	advID := p1NewAdvertiser(t, d)
	campaignID := p1NewCampaign(t, d, advID)
	creativeID := p1NewCreative(t, d, campaignID)

	wait := p1WaitForUpdate(t, d.Redis, campaignID)

	req := httptest.NewRequest("DELETE", fmt.Sprintf("/api/v1/creatives/%d", creativeID), nil)
	req.SetPathValue("id", fmt.Sprintf("%d", creativeID))
	w := httptest.NewRecorder()
	d.HandleDeleteCreative(w, req)

	if w.Code != http.StatusOK {
		t.Fatalf("want 200, got %d body=%s", w.Code, w.Body.String())
	}
	if !wait(2 * time.Second) {
		t.Fatalf("did not receive campaign:updates for campaign_id=%d within 2s", campaignID)
	}
}
```

- [ ] **Step 2: 确保 docker 栈起来**

Run: `docker compose up -d && docker compose ps`
Expected: postgres、redis 至少显示 `healthy` 或 `running`

- [ ] **Step 3: 运行测试,确认失败(TDD 红)**

Run: `go test -tags=e2e -count=1 -run TestCreativePubSub ./internal/handler/...`
Expected: 3 条测试都 FAIL,失败信息是 `did not receive campaign:updates for campaign_id=... within 2s`

- [ ] **Step 4: 不 commit**,进 Task P1.3 实现代码

---

### Task P1.3: 让 `HandleCreateCreative` 发 pub/sub(TDD 绿第 1 步)

**Files:**
- Modify: `internal/handler/campaign.go:440-493`

- [ ] **Step 1: 确认 import 有 `bidder`**

Run: `grep -n "internal/bidder" internal/handler/campaign.go`
Expected: 匹配到(becdc67 已加过)。如果没有,加 import `"github.com/heartgryphon/dsp/internal/bidder"`。

- [ ] **Step 2: 修改 `HandleCreateCreative`**

在 `HandleCreateCreative` 里 `WriteJSON(w, http.StatusCreated, ...)` 之前插入 pub/sub 发布。目标代码片段:

```go
	// Auto-approve creatives in development for faster iteration.
	// In production, creatives stay "pending" until admin reviews.
	status := "pending"
	if os.Getenv("ENV") != "production" {
		_ = d.Store.UpdateCreativeStatus(r.Context(), id, "approved")
		status = "approved"
	}

	if d.Redis != nil {
		bidder.NotifyCampaignUpdate(r.Context(), d.Redis, req.CampaignID, "updated")
	}

	WriteJSON(w, http.StatusCreated, map[string]any{"id": id, "status": status})
```

- [ ] **Step 3: 编译 + 跑 Create 测试**

Run: `go build ./... && go test -tags=e2e -count=1 -run TestCreativePubSub_Create ./internal/handler/...`
Expected: `TestCreativePubSub_Create` 通过;Update/Delete 仍然失败(符合预期,下面两个 task 分别修)。

---

### Task P1.4: 让 `HandleUpdateCreative` 发 pub/sub(TDD 绿第 2 步)

**Files:**
- Modify: `internal/handler/campaign.go:401-429`

- [ ] **Step 1: 修改 `HandleUpdateCreative`,先查后改再发**

把 `HandleUpdateCreative` 替换为:

```go
func (d *Deps) HandleUpdateCreative(w http.ResponseWriter, r *http.Request) {
	id, _ := strconv.ParseInt(r.PathValue("id"), 10, 64)
	var req struct {
		Name           string `json:"name"`
		AdType         string `json:"ad_type"`
		Format         string `json:"format"`
		Size           string `json:"size"`
		AdMarkup       string `json:"ad_markup"`
		DestinationURL string `json:"destination_url"`
	}
	if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
		WriteError(w, http.StatusBadRequest, "invalid request body")
		return
	}
	existing, err := d.Store.GetCreativeByID(r.Context(), id)
	if err != nil {
		WriteError(w, http.StatusNotFound, "creative not found")
		return
	}
	cr := &campaign.Creative{
		ID:             id,
		Name:           req.Name,
		AdType:         req.AdType,
		Format:         req.Format,
		Size:           req.Size,
		AdMarkup:       req.AdMarkup,
		DestinationURL: req.DestinationURL,
	}
	if err := d.Store.UpdateCreative(r.Context(), cr); err != nil {
		WriteError(w, http.StatusInternalServerError, err.Error())
		return
	}
	if d.Redis != nil {
		bidder.NotifyCampaignUpdate(r.Context(), d.Redis, existing.CampaignID, "updated")
	}
	WriteJSON(w, http.StatusOK, map[string]string{"status": "updated"})
}
```

关键点:
- 先 `GetCreativeByID` 拿 `existing.CampaignID` — 找不到返 404(之前是 500)
- 发布用 `existing.CampaignID`,不是 `cr.ID`

- [ ] **Step 2: 编译 + 跑 Update 测试**

Run: `go build ./... && go test -tags=e2e -count=1 -run TestCreativePubSub_Update ./internal/handler/...`
Expected: `TestCreativePubSub_Update` 通过。

---

### Task P1.5: 让 `HandleDeleteCreative` 发 pub/sub(TDD 绿第 3 步)

**Files:**
- Modify: `internal/handler/campaign.go:383-390`

- [ ] **Step 1: 修改 `HandleDeleteCreative`,先查后删再发**

把 `HandleDeleteCreative` 替换为:

```go
func (d *Deps) HandleDeleteCreative(w http.ResponseWriter, r *http.Request) {
	id, _ := strconv.ParseInt(r.PathValue("id"), 10, 64)
	existing, err := d.Store.GetCreativeByID(r.Context(), id)
	if err != nil {
		WriteError(w, http.StatusNotFound, "creative not found")
		return
	}
	if err := d.Store.DeleteCreative(r.Context(), id); err != nil {
		WriteError(w, http.StatusInternalServerError, err.Error())
		return
	}
	if d.Redis != nil {
		bidder.NotifyCampaignUpdate(r.Context(), d.Redis, existing.CampaignID, "updated")
	}
	WriteJSON(w, http.StatusOK, map[string]string{"status": "deleted"})
}
```

- [ ] **Step 2: 编译 + 跑 Delete 测试**

Run: `go build ./... && go test -tags=e2e -count=1 -run TestCreativePubSub_Delete ./internal/handler/...`
Expected: `TestCreativePubSub_Delete` 通过。

- [ ] **Step 3: 跑全部 3 条 creative pub/sub 测试确认**

Run: `go test -tags=e2e -count=1 -run TestCreativePubSub ./internal/handler/...`
Expected: 3 条全绿。

- [ ] **Step 4: 跑普通单测确认没回归**

Run: `go test ./...`
Expected: 全绿(注意 e2e 测试默认跳过,因为没 `-tags=e2e`)。

- [ ] **Step 5: Commit P1 完整修复**

```bash
git add internal/handler/campaign.go internal/handler/e2e_creative_pubsub_test.go
git commit -m "fix(handler): publish campaign:updates on creative CRUD

Close the three remaining gaps called out in docs/contracts/biz-engine.md §1:
POST/PUT/DELETE /api/v1/creatives now publish to campaign:updates with
action 'updated', matching becdc67's campaign handler pattern. UPDATE and
DELETE look up CampaignID via the new Store.GetCreativeByID before
mutating, and return 404 when the creative does not exist.

E2E regression tests under //go:build e2e subscribe to the channel and
verify each route publishes within 2s.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P1.6: P1 verification loop

**Files:** 无代码改动,执行验证循环。

本 Phase 的 verification 要走 **CLAUDE.md 的完整循环**,不是单独一次测试。下面列出每轮要走的步骤,直到整轮零问题才进 P2。上限 5 轮。

- [ ] **Step 1: Round 1 — requesting-code-review**

Invoke the `superpowers:requesting-code-review` skill on the P1 diff. Focus:
- `GetCreativeByID` 是否返回所有字段(特别是 `status`)
- 404 vs 500 的边界
- `d.Redis != nil` guard 是否每条路由都有
- pub/sub 发布发的是 `CampaignID`(整数)不是 creative id
- 测试是否订阅在发布之前(时序正确)

把 Critical/Important 问题就地修,修完回到 Step 1 重跑 review。

- [ ] **Step 2: Round 1 — verification-before-completion**

```bash
docker compose up -d
go test -tags=e2e -count=1 ./internal/handler/... 2>&1 | tee /tmp/p1-round1-e2e.log
go test ./... 2>&1 | tee /tmp/p1-round1-unit.log
```

Expected: 两条日志都没有 FAIL。

如果有 FAIL,就地修 → 回到 Step 1。

- [ ] **Step 3: Round 1 — 前端 /qa 跳过**

P1 没碰前端,本 Phase 的 /qa 步骤只做一次"smoke":
```
/qa 扫 /campaigns/[id] 的创意创建/编辑/删除交互
```
目的是验证 pub/sub 修复没副作用破坏前端流。

如果发现前端问题,**仅限"直接由 P1 修改触发的前端回归"才在 P1 修**;否则记入 `docs/qa/2026-04-14-biz-qa-report.md` 的前端章节留给 P4。

- [ ] **Step 4: 判定是否进 P2**

如果本 Round 走 Steps 1-3 有**任何**修复(代码或测试),必须从 Step 1 重跑下一轮。如果整轮零修复零问题,P1 完成,进 P2。

记录在 `docs/qa/2026-04-14-biz-qa-report.md`(如不存在,创建):
```markdown
## Phase 1 · Creative pub/sub 修复

- 走了 N 轮 verification loop
- 最终 commit: <hash>
- e2e 测试数量: 3
```

---

# Phase P2 · 后端 Go e2e 测试基建 + 全 handler 覆盖

**目标**:把路由 + 中间件从 `cmd/api/main.go` 抽到 `internal/handler/routes.go`,建测试 helper,按 §4 覆盖矩阵写 e2e 测试。

**Exit 标准**:`go test -tags=e2e -count=1 ./internal/handler/...` 全绿,覆盖所有 public/admin 路由;verification 循环零问题一轮。

---

### Task P2.1: 抽取路由 builder

**Files:**
- Create: `internal/handler/routes.go`
- Modify: `cmd/api/main.go:123-202`

- [ ] **Step 1: 创建 `internal/handler/routes.go`**

```go
package handler

import (
	"context"
	"fmt"
	"net/http"
	"time"

	"github.com/heartgryphon/dsp/internal/auth"
	"github.com/heartgryphon/dsp/internal/campaign"
	"github.com/heartgryphon/dsp/internal/config"
	"github.com/heartgryphon/dsp/internal/observability"
	"github.com/heartgryphon/dsp/internal/ratelimit"
	"github.com/prometheus/client_golang/prometheus/promhttp"
)

// BuildPublicMux returns the bare public router (no middleware).
// Use in tests that want to bypass auth/ratelimit.
func BuildPublicMux(d *Deps) *http.ServeMux {
	mux := http.NewServeMux()
	mux.HandleFunc("POST /api/v1/advertisers", d.HandleCreateAdvertiser)
	mux.HandleFunc("GET /api/v1/advertisers/{id}", d.HandleGetAdvertiser)
	mux.HandleFunc("POST /api/v1/campaigns", d.HandleCreateCampaign)
	mux.HandleFunc("GET /api/v1/campaigns", d.HandleListCampaigns)
	mux.HandleFunc("GET /api/v1/campaigns/{id}", d.HandleGetCampaign)
	mux.HandleFunc("PUT /api/v1/campaigns/{id}", d.HandleUpdateCampaign)
	mux.HandleFunc("POST /api/v1/campaigns/{id}/start", d.HandleStartCampaign)
	mux.HandleFunc("POST /api/v1/campaigns/{id}/pause", d.HandlePauseCampaign)
	mux.HandleFunc("GET /api/v1/campaigns/{id}/creatives", d.HandleListCreatives)
	mux.HandleFunc("POST /api/v1/creatives", d.HandleCreateCreative)
	mux.HandleFunc("PUT /api/v1/creatives/{id}", d.HandleUpdateCreative)
	mux.HandleFunc("DELETE /api/v1/creatives/{id}", d.HandleDeleteCreative)
	mux.HandleFunc("GET /api/v1/ad-types", d.HandleAdTypes)
	mux.HandleFunc("GET /api/v1/billing-models", d.HandleBillingModels)
	mux.HandleFunc("GET /api/v1/reports/campaign/{id}/stats", d.HandleCampaignStats)
	mux.HandleFunc("GET /api/v1/reports/campaign/{id}/hourly", d.HandleHourlyStats)
	mux.HandleFunc("GET /api/v1/reports/campaign/{id}/geo", d.HandleGeoBreakdown)
	mux.HandleFunc("GET /api/v1/reports/campaign/{id}/bids", d.HandleBidTransparency)
	mux.HandleFunc("GET /api/v1/reports/campaign/{id}/attribution", d.HandleAttribution)
	mux.HandleFunc("GET /api/v1/reports/campaign/{id}/simulate", d.HandleBidSimulate)
	mux.HandleFunc("GET /api/v1/reports/overview", d.HandleOverviewStats)
	mux.HandleFunc("GET /api/v1/export/campaign/{id}/stats", d.HandleExportCampaignCSV)
	mux.HandleFunc("GET /api/v1/export/campaign/{id}/bids", d.HandleExportBidsCSV)
	mux.HandleFunc("GET /api/v1/audit-log", d.HandleMyAuditLog)
	mux.HandleFunc("GET /api/v1/analytics/stream", d.HandleAnalyticsStream)
	mux.HandleFunc("GET /api/v1/analytics/snapshot", d.HandleAnalyticsSnapshot)
	mux.HandleFunc("POST /api/v1/billing/topup", d.HandleTopUp)
	mux.HandleFunc("GET /api/v1/billing/transactions", d.HandleTransactions)
	mux.HandleFunc("GET /api/v1/billing/balance/{id}", d.HandleBalance)
	mux.HandleFunc("POST /api/v1/upload", d.HandleUpload)
	mux.Handle("/uploads/", http.StripPrefix("/uploads/", UploadFileServer()))
	mux.HandleFunc("POST /api/v1/register", d.HandleRegister)
	mux.HandleFunc("GET /api/v1/docs", d.HandleAPIDocs)
	mux.HandleFunc("GET /health", func(w http.ResponseWriter, r *http.Request) {
		fmt.Fprintf(w, `{"status":"ok","time":"%s"}`, time.Now().UTC().Format(time.RFC3339))
	})
	return mux
}

// BuildAdminMux returns the bare admin router (no auth middleware).
// Use in tests that want to bypass the X-Admin-Token check.
func BuildAdminMux(d *Deps) *http.ServeMux {
	mux := http.NewServeMux()
	mux.HandleFunc("GET /internal/active-campaigns", d.HandleActiveCampaigns)
	mux.HandleFunc("GET /api/v1/admin/registrations", d.HandleListRegistrations)
	mux.HandleFunc("POST /api/v1/admin/registrations/{id}/approve", d.HandleApproveRegistration)
	mux.HandleFunc("POST /api/v1/admin/registrations/{id}/reject", d.HandleRejectRegistration)
	mux.HandleFunc("GET /api/v1/admin/health", d.HandleSystemHealth)
	mux.HandleFunc("GET /api/v1/admin/creatives", d.HandleListCreativesForReview)
	mux.HandleFunc("POST /api/v1/admin/creatives/{id}/approve", d.HandleApproveCreative)
	mux.HandleFunc("POST /api/v1/admin/creatives/{id}/reject", d.HandleRejectCreative)
	mux.HandleFunc("POST /api/v1/admin/circuit-break", d.HandleCircuitBreak)
	mux.HandleFunc("POST /api/v1/admin/circuit-reset", d.HandleCircuitReset)
	mux.HandleFunc("GET /api/v1/admin/circuit-status", d.HandleCircuitStatus)
	mux.HandleFunc("GET /api/v1/admin/advertisers", d.HandleListAdvertisers)
	mux.HandleFunc("POST /api/v1/admin/topup", d.HandleAdminTopUp)
	mux.HandleFunc("POST /api/v1/admin/invite-codes", d.HandleCreateInviteCode)
	mux.HandleFunc("GET /api/v1/admin/invite-codes", d.HandleListInviteCodes)
	mux.HandleFunc("GET /api/v1/admin/audit-log", d.HandleAuditLog)
	return mux
}

// BuildPublicHandler returns the full production public handler chain:
// CORS → RequestID → Logging → AuthExemption → RateLimit → APIKey → publicMux.
func BuildPublicHandler(cfg *config.Config, d *Deps, store *campaign.Store) http.Handler {
	publicMux := BuildPublicMux(d)
	apiKeyLookup := func(ctx context.Context, key string) (int64, string, string, error) {
		adv, err := store.GetAdvertiserByAPIKey(ctx, key)
		if err != nil {
			return 0, "", "", err
		}
		return adv.ID, adv.CompanyName, adv.ContactEmail, nil
	}
	limiter := ratelimit.New(d.Redis)
	authed := auth.APIKeyMiddleware(apiKeyLookup)(publicMux)
	rateLimited := ratelimit.Middleware(limiter, ratelimit.APIKeyFunc, 100, time.Minute)(authed)
	withExemption := WithAuthExemption(rateLimited, publicMux)
	return WithCORS(cfg, observability.RequestIDMiddleware(observability.LoggingMiddleware(withExemption)))
}

// BuildInternalHandler returns the full internal (admin) handler chain:
// CORS → Logging → admin auth (for /internal/ and /api/v1/admin/) + /metrics + /health.
func BuildInternalHandler(cfg *config.Config, d *Deps) http.Handler {
	adminMux := BuildAdminMux(d)
	internalMux := http.NewServeMux()
	internalMux.Handle("GET /metrics", promhttp.Handler())
	internalMux.Handle("/internal/", AdminAuthMiddleware(adminMux))
	internalMux.Handle("/api/v1/admin/", AdminAuthMiddleware(adminMux))
	internalMux.HandleFunc("GET /health", func(w http.ResponseWriter, r *http.Request) {
		fmt.Fprintf(w, `{"status":"ok","port":"internal","time":"%s"}`, time.Now().UTC().Format(time.RFC3339))
	})
	return WithCORS(cfg, observability.LoggingMiddleware(internalMux))
}
```

- [ ] **Step 2: 改 `cmd/api/main.go` 用 builders**

把 `cmd/api/main.go:123-202` 的块替换为:

```go
	publicSrv := &http.Server{
		Addr:    ":" + cfg.APIPort,
		Handler: handler.BuildPublicHandler(cfg, h, store),
	}
	internalSrv := &http.Server{
		Addr:    ":" + cfg.InternalPort,
		Handler: handler.BuildInternalHandler(cfg, h),
	}
```

同时删除 `cmd/api/main.go` 里已不再用的 imports:`auth`, `ratelimit`, `promhttp` 应该保留在 handler 包里不是 main。去掉 main 里多余的 import。

- [ ] **Step 3: 编译**

Run: `go build ./cmd/api/... && go build ./...`
Expected: 无错误输出。

- [ ] **Step 4: 启动栈 + 健康检查**

```bash
docker compose up -d
sleep 3
curl -s http://localhost:19181/health && echo
curl -s http://localhost:19182/health && echo
```
Expected: 两个 `{"status":"ok",...}` 响应。

- [ ] **Step 5: 跑现有单测确认没回归**

Run: `go test ./...`
Expected: 全绿。

- [ ] **Step 6: 跑 P1 的 e2e 测试确认 helper 没坏**

Run: `go test -tags=e2e -count=1 -run TestCreativePubSub ./internal/handler/...`
Expected: 3 条全绿。

- [ ] **Step 7: Commit**

```bash
git add internal/handler/routes.go cmd/api/main.go
git commit -m "refactor(handler): extract route builders from cmd/api/main

Move the public/admin mux wiring and middleware chain into
internal/handler/routes.go so e2e tests can import real routers.
cmd/api/main.go now just calls BuildPublicHandler / BuildInternalHandler.
No behavior change.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P2.2: 建 `e2e_support_test.go` 测试基建

**Files:**
- Create: `internal/handler/e2e_support_test.go`
- Modify: `internal/handler/e2e_creative_pubsub_test.go`(迁移到 helper)

- [ ] **Step 1: 写 `e2e_support_test.go`**

Create `internal/handler/e2e_support_test.go`:

```go
//go:build e2e
// +build e2e

package handler_test

import (
	"bytes"
	"context"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"net/http/httptest"
	"os"
	"testing"
	"time"

	"github.com/ClickHouse/clickhouse-go/v2"
	chdriver "github.com/ClickHouse/clickhouse-go/v2/lib/driver"
	"github.com/heartgryphon/dsp/internal/audit"
	"github.com/heartgryphon/dsp/internal/billing"
	"github.com/heartgryphon/dsp/internal/budget"
	"github.com/heartgryphon/dsp/internal/campaign"
	"github.com/heartgryphon/dsp/internal/handler"
	"github.com/heartgryphon/dsp/internal/registration"
	"github.com/heartgryphon/dsp/internal/reporting"
	"github.com/jackc/pgx/v5/pgxpool"
	"github.com/redis/go-redis/v9"
)

// --- env + defaults ---

func envOr(k, d string) string {
	if v := os.Getenv(k); v != "" {
		return v
	}
	return d
}

func pgDSN() string    { return envOr("DSP_E2E_PG_DSN", "postgres://dsp:dsp@localhost:16432/dsp?sslmode=disable") }
func redisAddr() string { return envOr("DSP_E2E_REDIS_ADDR", "localhost:17380") }
func chAddr() string    { return envOr("DSP_E2E_CH_ADDR", "localhost:19124") }
func adminToken() string { return envOr("DSP_E2E_ADMIN_TOKEN", "admin-secret") }

// mustPool returns a raw pgxpool for tests that need to issue ad-hoc SQL
// (e.g. looking up ids by email). Caller gets a cleanup via t.Cleanup.
func mustPool(t *testing.T) *pgxpool.Pool {
	t.Helper()
	ctx := context.Background()
	db, err := pgxpool.New(ctx, pgDSN())
	if err != nil {
		t.Skipf("postgres not reachable (%v)", err)
	}
	if err := db.Ping(ctx); err != nil {
		db.Close()
		t.Skipf("postgres ping failed (%v)", err)
	}
	t.Cleanup(func() { db.Close() })
	return db
}

// --- test deps ---

func mustDeps(t *testing.T) *handler.Deps {
	t.Helper()
	ctx := context.Background()
	db, err := pgxpool.New(ctx, pgDSN())
	if err != nil {
		t.Skipf("postgres not reachable (%v) — run `docker compose up -d`", err)
	}
	if err := db.Ping(ctx); err != nil {
		t.Skipf("postgres ping failed (%v)", err)
	}
	rdb := redis.NewClient(&redis.Options{Addr: redisAddr()})
	if err := rdb.Ping(ctx).Err(); err != nil {
		db.Close()
		t.Skipf("redis not reachable (%v)", err)
	}
	rs, err := reporting.NewStore(chAddr(), "", "")
	if err != nil {
		t.Logf("clickhouse not reachable (%v) — report tests will skip", err)
		rs = nil
	}
	t.Cleanup(func() {
		_ = rdb.Close()
		db.Close()
	})
	return &handler.Deps{
		Store:       campaign.NewStore(db),
		ReportStore: rs,
		BillingSvc:  billing.New(db),
		RegSvc:      registration.New(db),
		BudgetSvc:   budget.New(rdb),
		Redis:       rdb,
		AuditLog:    audit.NewLogger(db),
	}
}

// --- fixtures ---

// newAdvertiser creates an advertiser with a unique email, returning (id, apiKey).
func newAdvertiser(t *testing.T, d *handler.Deps) (int64, string) {
	t.Helper()
	key := handler.GenerateAPIKey()
	email := fmt.Sprintf("qa-%s-%d@test.local", t.Name(), time.Now().UnixNano())
	adv := &campaign.Advertiser{
		CompanyName:  "QA-" + t.Name(),
		ContactEmail: email,
		APIKey:       key,
	}
	id, err := d.Store.CreateAdvertiser(context.Background(), adv)
	if err != nil {
		t.Fatalf("create advertiser: %v", err)
	}
	// Give a starting balance so campaign start / topup tests don't fail on balance.
	if err := d.BillingSvc.AdminCredit(context.Background(), id, 100000, "qa fixture"); err != nil {
		t.Logf("fixture topup failed (non-fatal): %v", err)
	}
	return id, key
}

// newCampaign creates a draft campaign for the given advertiser.
func newCampaign(t *testing.T, d *handler.Deps, advID int64) int64 {
	t.Helper()
	c := &campaign.Campaign{
		AdvertiserID: advID,
		Name:         "QA-" + t.Name(),
		BidCPMCents:  100,
		DailyBudget:  10000,
		Status:       "draft",
		BillingModel: "cpm",
	}
	id, err := d.Store.CreateCampaign(context.Background(), c)
	if err != nil {
		t.Fatalf("create campaign: %v", err)
	}
	return id
}

// newCreative creates a creative for the given campaign.
func newCreative(t *testing.T, d *handler.Deps, campaignID int64) int64 {
	t.Helper()
	cr := &campaign.Creative{
		CampaignID:     campaignID,
		Name:           "QA-" + t.Name(),
		AdType:         "banner",
		Format:         "image",
		Size:           "300x250",
		AdMarkup:       "<a>ad</a>",
		DestinationURL: "https://example.com",
	}
	id, err := d.Store.CreateCreative(context.Background(), cr)
	if err != nil {
		t.Fatalf("create creative: %v", err)
	}
	return id
}

// --- request helpers ---

func authedReq(t *testing.T, method, path string, body any, apiKey string) *http.Request {
	t.Helper()
	var buf io.Reader
	if body != nil {
		b, _ := json.Marshal(body)
		buf = bytes.NewReader(b)
	}
	req := httptest.NewRequest(method, path, buf)
	if apiKey != "" {
		req.Header.Set("X-API-Key", apiKey)
	}
	if body != nil {
		req.Header.Set("Content-Type", "application/json")
	}
	return req
}

func adminReq(t *testing.T, method, path string, body any) *http.Request {
	t.Helper()
	var buf io.Reader
	if body != nil {
		b, _ := json.Marshal(body)
		buf = bytes.NewReader(b)
	}
	req := httptest.NewRequest(method, path, buf)
	req.Header.Set("X-Admin-Token", adminToken())
	if body != nil {
		req.Header.Set("Content-Type", "application/json")
	}
	return req
}

// execPublic calls the bare public mux (no middleware).
// Use this for authz-bypass tests that only exercise handler logic.
func execPublic(t *testing.T, d *handler.Deps, req *http.Request) *httptest.ResponseRecorder {
	t.Helper()
	mux := handler.BuildPublicMux(d)
	w := httptest.NewRecorder()
	mux.ServeHTTP(w, req)
	return w
}

// execPublicWithAuth calls the public mux with the real auth middleware
// (but without rate-limit). Use this for authz-aware tests.
func execPublicWithAuth(t *testing.T, d *handler.Deps, store *campaign.Store, req *http.Request) *httptest.ResponseRecorder {
	t.Helper()
	mux := handler.BuildPublicMux(d)
	lookup := func(ctx context.Context, key string) (int64, string, string, error) {
		adv, err := store.GetAdvertiserByAPIKey(ctx, key)
		if err != nil {
			return 0, "", "", err
		}
		return adv.ID, adv.CompanyName, adv.ContactEmail, nil
	}
	// auth.APIKeyMiddleware signature from internal/auth/apikey.go
	authed := authMiddleware(lookup, mux)
	withExemption := handler.WithAuthExemption(authed, mux)
	w := httptest.NewRecorder()
	withExemption.ServeHTTP(w, req)
	return w
}

// authMiddleware is a thin shim so we don't import auth in every test file.
func authMiddleware(lookup func(ctx context.Context, key string) (int64, string, string, error), next http.Handler) http.Handler {
	return authMiddlewareImpl(lookup, next)
}

// execAdmin calls the bare admin mux (no admin token check).
// Use this for handler-logic tests. Pair with execAdminWithAuth for authz checks.
func execAdmin(t *testing.T, d *handler.Deps, req *http.Request) *httptest.ResponseRecorder {
	t.Helper()
	mux := handler.BuildAdminMux(d)
	w := httptest.NewRecorder()
	mux.ServeHTTP(w, req)
	return w
}

// execAdminWithAuth calls the admin mux behind AdminAuthMiddleware so 401 branches can be tested.
func execAdminWithAuth(t *testing.T, d *handler.Deps, req *http.Request) *httptest.ResponseRecorder {
	t.Helper()
	mux := handler.BuildAdminMux(d)
	withAuth := handler.AdminAuthMiddleware(mux)
	w := httptest.NewRecorder()
	withAuth.ServeHTTP(w, req)
	return w
}

// --- pub/sub subscribe helper ---

// subscribeUpdates returns a wait function that reports whether a campaign:updates
// message for the given campaign_id arrived within d. Subscription is established
// before the function returns, so callers can trigger handlers immediately after.
func subscribeUpdates(t *testing.T, rdb *redis.Client, campaignID int64) func(time.Duration) bool {
	t.Helper()
	ctx := context.Background()
	pubsub := rdb.Subscribe(ctx, "campaign:updates")
	if _, err := pubsub.Receive(ctx); err != nil {
		t.Fatalf("subscribe confirm: %v", err)
	}
	got := make(chan struct{}, 1)
	go func() {
		for msg := range pubsub.Channel() {
			var payload struct {
				CampaignID int64  `json:"campaign_id"`
				Action     string `json:"action"`
			}
			if err := json.Unmarshal([]byte(msg.Payload), &payload); err != nil {
				continue
			}
			if payload.CampaignID == campaignID {
				select {
				case got <- struct{}{}:
				default:
				}
				return
			}
		}
	}()
	t.Cleanup(func() { _ = pubsub.Close() })
	return func(d time.Duration) bool {
		select {
		case <-got:
			return true
		case <-time.After(d):
			return false
		}
	}
}

// --- ClickHouse fixture helper ---

// mustCHConn opens a raw clickhouse-go conn for fixture inserts.
func mustCHConn(t *testing.T) chdriver.Conn {
	t.Helper()
	conn, err := clickhouse.Open(&clickhouse.Options{
		Addr: []string{chAddr()},
		Auth: clickhouse.Auth{Database: "default"},
	})
	if err != nil {
		t.Skipf("clickhouse not reachable: %v", err)
	}
	if err := conn.Ping(context.Background()); err != nil {
		t.Skipf("clickhouse ping failed: %v", err)
	}
	t.Cleanup(func() { _ = conn.Close() })
	return conn
}

// insertBidLog inserts N synthetic bid_log rows for (advertiserID, campaignID, creativeID).
// eventType: "bid" | "win" | "impression" | "click" | "conversion"
// All rows share event_time = now, geo_country = "US", device_os = "iOS".
func insertBidLog(t *testing.T, conn chdriver.Conn, advertiserID, campaignID, creativeID int64, eventType string, n int) {
	t.Helper()
	typeCode := map[string]int{"bid": 1, "win": 2, "loss": 3, "impression": 4, "click": 5, "conversion": 6}[eventType]
	if typeCode == 0 {
		t.Fatalf("unknown event type: %s", eventType)
	}
	batch, err := conn.PrepareBatch(context.Background(),
		`INSERT INTO bid_log (event_date, event_time, campaign_id, creative_id, advertiser_id, exchange_id, request_id, geo_country, device_os, device_id, bid_price_cents, clear_price_cents, charge_cents, event_type, loss_reason)`)
	if err != nil {
		t.Fatalf("prepare batch: %v", err)
	}
	now := time.Now()
	for i := 0; i < n; i++ {
		if err := batch.Append(
			now, now, campaignID, creativeID, advertiserID,
			"qa-exchange", fmt.Sprintf("%s-%d-%d", t.Name(), now.UnixNano(), i),
			"US", "iOS", fmt.Sprintf("device-%d", i),
			uint32(100), uint32(80), uint32(80), uint8(typeCode), "",
		); err != nil {
			t.Fatalf("append row %d: %v", i, err)
		}
	}
	if err := batch.Send(); err != nil {
		t.Fatalf("send batch: %v", err)
	}
}

// decodeJSON is a convenience wrapper for reading response bodies.
func decodeJSON(t *testing.T, r *httptest.ResponseRecorder, out any) {
	t.Helper()
	if err := json.NewDecoder(r.Body).Decode(out); err != nil {
		t.Fatalf("decode json (body=%s): %v", r.Body.String(), err)
	}
}
```

**注意 — 两个编译期依赖要在步骤 2 解决**:
1. `authMiddlewareImpl` — helper 里调用 `authMiddleware` 实际走 `auth.APIKeyMiddleware`,需要 shim。
2. `handler.Deps.HandleCreateAdvertiser` 等 handler 方法必须在 handler 包导出。

- [ ] **Step 2: 加 auth shim(单独文件避免污染)**

Create `internal/handler/e2e_auth_shim_test.go`:

```go
//go:build e2e
// +build e2e

package handler_test

import (
	"context"
	"net/http"

	"github.com/heartgryphon/dsp/internal/auth"
)

func authMiddlewareImpl(lookup func(ctx context.Context, key string) (int64, string, string, error), next http.Handler) http.Handler {
	return auth.APIKeyMiddleware(lookup)(next)
}
```

- [ ] **Step 3: 迁移 P1 内联代码到 helper**

把 `internal/handler/e2e_creative_pubsub_test.go` 里 `p1Deps` / `p1NewAdvertiser` / `p1NewCampaign` / `p1NewCreative` / `p1WaitForUpdate` 这些内联辅助函数**全部删掉**,测试体里的调用替换为 `mustDeps(t)` / `newAdvertiser(t, d)` / `newCampaign(t, d, advID)` / `newCreative(t, d, campaignID)` / `subscribeUpdates(t, d.Redis, campaignID)`。

替换示例(`TestCreativePubSub_Create`):

```go
func TestCreativePubSub_Create(t *testing.T) {
	d := mustDeps(t)
	advID, _ := newAdvertiser(t, d)
	campaignID := newCampaign(t, d, advID)

	wait := subscribeUpdates(t, d.Redis, campaignID)

	body := map[string]any{
		"campaign_id":     campaignID,
		"name":            "c1",
		"ad_type":         "banner",
		"format":          "image",
		"size":            "300x250",
		"ad_markup":       "<a>ad</a>",
		"destination_url": "https://example.com",
	}
	req := authedReq(t, "POST", "/api/v1/creatives", body, "")
	w := execPublic(t, d, req)

	if w.Code != http.StatusCreated {
		t.Fatalf("want 201, got %d body=%s", w.Code, w.Body.String())
	}
	if !wait(2 * time.Second) {
		t.Fatalf("did not receive campaign:updates for campaign_id=%d within 2s", campaignID)
	}
}
```

同理改 Update / Delete(PUT/DELETE 测试里不需要 `authedReq` 的 `body`,直接用 `execPublic`,因为 `BuildPublicMux` 不挂 auth 中间件)。

- [ ] **Step 4: 编译 + 跑 P1 测试确认迁移没破坏**

Run: `go test -tags=e2e -count=1 -run TestCreativePubSub ./internal/handler/...`
Expected: 3 条仍然全绿。

- [ ] **Step 5: Commit**

```bash
git add internal/handler/e2e_support_test.go internal/handler/e2e_auth_shim_test.go internal/handler/e2e_creative_pubsub_test.go
git commit -m "test(handler): add e2e test support harness

Shared helpers for all e2e test files: mustDeps (connects to real
pg/redis/clickhouse with env-configurable addrs), newAdvertiser /
newCampaign / newCreative fixtures (unique per t.Name()), authedReq /
adminReq request builders, execPublic / execAdmin runners against the
BuildPublicMux / BuildAdminMux extracted in the previous commit,
subscribeUpdates for pub/sub assertions, and insertBidLog for ClickHouse
report fixtures.

P1's creative pub/sub tests migrate off their inline helpers.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P2.3: Advertiser + billing + register 覆盖

**Files:**
- Create: `internal/handler/e2e_public_advertiser_test.go`

**覆盖**:POST/GET `/api/v1/advertisers`、POST `/api/v1/register`、POST `/api/v1/billing/topup`、GET `/api/v1/billing/transactions`、GET `/api/v1/billing/balance/{id}`。

- [ ] **Step 1: 写测试文件**

Create `internal/handler/e2e_public_advertiser_test.go`:

```go
//go:build e2e
// +build e2e

package handler_test

import (
	"context"
	"fmt"
	"net/http"
	"testing"
)

func TestAdvertiser_CreateAndGet(t *testing.T) {
	d := mustDeps(t)

	body := map[string]any{
		"company_name":  "acme-" + t.Name(),
		"contact_email": fmt.Sprintf("create-%d@test.local", nowNano()),
	}
	req := authedReq(t, "POST", "/api/v1/advertisers", body, "")
	w := execPublic(t, d, req)
	if w.Code != http.StatusCreated {
		t.Fatalf("POST /advertisers want 201, got %d: %s", w.Code, w.Body.String())
	}
	var created struct {
		ID     int64  `json:"id"`
		APIKey string `json:"api_key"`
	}
	decodeJSON(t, w, &created)
	if created.ID == 0 || created.APIKey == "" {
		t.Fatalf("missing id/api_key in response: %+v", created)
	}

	req2 := authedReq(t, "GET", fmt.Sprintf("/api/v1/advertisers/%d", created.ID), nil, created.APIKey)
	w2 := execPublic(t, d, req2)
	if w2.Code != http.StatusOK {
		t.Fatalf("GET /advertisers/{id} want 200, got %d", w2.Code)
	}
}

func TestAdvertiser_Create_MissingFields_400(t *testing.T) {
	d := mustDeps(t)
	body := map[string]any{} // no company_name, no email
	req := authedReq(t, "POST", "/api/v1/advertisers", body, "")
	w := execPublic(t, d, req)
	if w.Code != http.StatusBadRequest {
		t.Fatalf("want 400, got %d", w.Code)
	}
}

func TestAdvertiser_Get_NotFound_404(t *testing.T) {
	d := mustDeps(t)
	req := authedReq(t, "GET", "/api/v1/advertisers/99999999", nil, "")
	w := execPublic(t, d, req)
	if w.Code != http.StatusNotFound {
		t.Fatalf("want 404, got %d", w.Code)
	}
}

func TestBilling_TopUp_UpdatesBalance(t *testing.T) {
	d := mustDeps(t)
	advID, apiKey := newAdvertiser(t, d)

	// get initial balance
	req := authedReq(t, "GET", fmt.Sprintf("/api/v1/billing/balance/%d", advID), nil, apiKey)
	w := execPublic(t, d, req)
	if w.Code != http.StatusOK {
		t.Fatalf("get balance: want 200, got %d", w.Code)
	}
	var before struct {
		BalanceCents int64 `json:"balance_cents"`
	}
	decodeJSON(t, w, &before)

	// topup 500 cents
	top := map[string]any{"advertiser_id": advID, "amount_cents": 500}
	wTop := execPublic(t, d, authedReq(t, "POST", "/api/v1/billing/topup", top, apiKey))
	if wTop.Code != http.StatusOK && wTop.Code != http.StatusCreated {
		t.Fatalf("topup: want 200/201, got %d: %s", wTop.Code, wTop.Body.String())
	}

	// balance increases
	wAfter := execPublic(t, d, authedReq(t, "GET", fmt.Sprintf("/api/v1/billing/balance/%d", advID), nil, apiKey))
	var after struct {
		BalanceCents int64 `json:"balance_cents"`
	}
	decodeJSON(t, wAfter, &after)
	if after.BalanceCents < before.BalanceCents+500 {
		t.Fatalf("balance did not increase by 500: before=%d after=%d", before.BalanceCents, after.BalanceCents)
	}
}

func TestBilling_TopUp_NegativeAmount_400(t *testing.T) {
	d := mustDeps(t)
	advID, apiKey := newAdvertiser(t, d)
	top := map[string]any{"advertiser_id": advID, "amount_cents": -100}
	w := execPublic(t, d, authedReq(t, "POST", "/api/v1/billing/topup", top, apiKey))
	if w.Code != http.StatusBadRequest {
		t.Fatalf("want 400, got %d body=%s", w.Code, w.Body.String())
	}
}

func TestBilling_Transactions_ListsTopUp(t *testing.T) {
	d := mustDeps(t)
	advID, apiKey := newAdvertiser(t, d)
	_ = execPublic(t, d, authedReq(t, "POST", "/api/v1/billing/topup", map[string]any{
		"advertiser_id": advID,
		"amount_cents":  777,
	}, apiKey))

	w := execPublic(t, d, authedReq(t, "GET", "/api/v1/billing/transactions", nil, apiKey))
	if w.Code != http.StatusOK {
		t.Fatalf("want 200, got %d", w.Code)
	}
	if !contains(w.Body.String(), "777") {
		t.Fatalf("expected to see 777 in transactions body: %s", w.Body.String())
	}
}

func TestRegister_ValidInviteCode(t *testing.T) {
	d := mustDeps(t)
	code, err := d.RegSvc.CreateInviteCode(context.Background(), "qa-"+t.Name(), 10, nil)
	if err != nil {
		t.Fatalf("create invite code: %v", err)
	}

	body := map[string]any{
		"company_name":  "reg-" + t.Name(),
		"contact_email": fmt.Sprintf("reg-%d@test.local", nowNano()),
		"invite_code":   code,
	}
	w := execPublic(t, d, authedReq(t, "POST", "/api/v1/register", body, ""))
	if w.Code != http.StatusCreated && w.Code != http.StatusOK {
		t.Fatalf("want 200/201, got %d: %s", w.Code, w.Body.String())
	}
}

func TestRegister_InvalidInviteCode_400(t *testing.T) {
	d := mustDeps(t)
	body := map[string]any{
		"company_name":  "reg-" + t.Name(),
		"contact_email": fmt.Sprintf("reg-%d@test.local", nowNano()),
		"invite_code":   "INVALID-CODE-XYZ",
	}
	w := execPublic(t, d, authedReq(t, "POST", "/api/v1/register", body, ""))
	if w.Code != http.StatusBadRequest {
		t.Fatalf("want 400, got %d body=%s", w.Code, w.Body.String())
	}
}
```

- [ ] **Step 2: 在 `e2e_support_test.go` 底部加两个小工具函数**

```go
func nowNano() int64 { return time.Now().UnixNano() }
func contains(s, sub string) bool { return strings.Contains(s, sub) }
```

并在文件头 import 加上 `"strings"`。

- [ ] **Step 3: 跑测试**

Run: `go test -tags=e2e -count=1 -run 'TestAdvertiser|TestBilling|TestRegister' ./internal/handler/...`
Expected: 全绿。

**如果某条失败**,走 TDD 修复流程:
1. 识别失败原因(handler 400/404/500 分支分类不对?字段解析问题?balance 返回值格式不对?)
2. 先让测试精准表达**正确**的期望值(不要改测试去迁就坏行为)
3. 修 handler 让测试绿
4. 把修复记入 `docs/qa/2026-04-14-biz-qa-report.md` 的 B### bug 列表

- [ ] **Step 4: Commit**

```bash
git add internal/handler/e2e_public_advertiser_test.go internal/handler/e2e_support_test.go
git commit -m "test(handler): add e2e coverage for advertiser + billing + register

Happy path + error cases for:
- POST/GET /api/v1/advertisers
- POST /api/v1/billing/topup (incl. negative amount 400)
- GET /api/v1/billing/transactions
- GET /api/v1/billing/balance/{id}
- POST /api/v1/register (valid and invalid invite code)

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P2.4: Campaign + start/pause 覆盖

**Files:**
- Create: `internal/handler/e2e_public_campaign_test.go`

**覆盖**:POST/GET/PUT `/api/v1/campaigns`、start/pause 两个动作、状态机校验、pub/sub 断言。

- [ ] **Step 1: 写测试文件**

Create `internal/handler/e2e_public_campaign_test.go`:

```go
//go:build e2e
// +build e2e

package handler_test

import (
	"fmt"
	"net/http"
	"testing"
	"time"
)

func TestCampaign_CreatePublishesUpdated(t *testing.T) {
	d := mustDeps(t)
	advID, apiKey := newAdvertiser(t, d)

	// We don't know the campaign_id yet, so subscribe to all and match by name.
	// Easier path: create, read the id from the response, THEN verify pub/sub was
	// published. Since NotifyCampaignUpdate is fire-and-forget we can't catch a
	// past message. Solution: use subscribeUpdatesAny and check after.
	// For this test we'll trust becdc67's existing pattern and verify via bidder
	// loader indirectly — OR we do a two-phase: create first, then subscribe +
	// update. Simpler: do update below instead.

	body := map[string]any{
		"advertiser_id":  advID,
		"name":           "c-" + t.Name(),
		"bid_cpm_cents":  100,
		"daily_budget":   10000,
		"billing_model":  "cpm",
		"targeting":      map[string]any{},
	}
	w := execPublic(t, d, authedReq(t, "POST", "/api/v1/campaigns", body, apiKey))
	if w.Code != http.StatusCreated {
		t.Fatalf("want 201, got %d body=%s", w.Code, w.Body.String())
	}
	var created struct {
		ID int64 `json:"id"`
	}
	decodeJSON(t, w, &created)
	if created.ID == 0 {
		t.Fatal("missing id in response")
	}
}

func TestCampaign_UpdatePublishesUpdated(t *testing.T) {
	d := mustDeps(t)
	advID, apiKey := newAdvertiser(t, d)
	campaignID := newCampaign(t, d, advID)

	wait := subscribeUpdates(t, d.Redis, campaignID)

	body := map[string]any{
		"name":          "renamed-" + t.Name(),
		"bid_cpm_cents": 200,
		"daily_budget":  20000,
	}
	w := execPublic(t, d, authedReq(t, "PUT", fmt.Sprintf("/api/v1/campaigns/%d", campaignID), body, apiKey))
	if w.Code != http.StatusOK {
		t.Fatalf("want 200, got %d body=%s", w.Code, w.Body.String())
	}
	if !wait(2 * time.Second) {
		t.Fatal("did not receive campaign:updates updated")
	}
}

func TestCampaign_Start_PublishesActivated(t *testing.T) {
	d := mustDeps(t)
	advID, apiKey := newAdvertiser(t, d)
	campaignID := newCampaign(t, d, advID)

	waitActivated := subscribeUpdatesAction(t, d.Redis, campaignID, "activated")

	w := execPublic(t, d, authedReq(t, "POST", fmt.Sprintf("/api/v1/campaigns/%d/start", campaignID), nil, apiKey))
	if w.Code != http.StatusOK {
		t.Fatalf("start want 200, got %d body=%s", w.Code, w.Body.String())
	}
	if !waitActivated(2 * time.Second) {
		t.Fatal("did not receive activated")
	}
}

func TestCampaign_Pause_PublishesPaused(t *testing.T) {
	d := mustDeps(t)
	advID, apiKey := newAdvertiser(t, d)
	campaignID := newCampaign(t, d, advID)

	// first start
	_ = execPublic(t, d, authedReq(t, "POST", fmt.Sprintf("/api/v1/campaigns/%d/start", campaignID), nil, apiKey))

	waitPaused := subscribeUpdatesAction(t, d.Redis, campaignID, "paused")
	w := execPublic(t, d, authedReq(t, "POST", fmt.Sprintf("/api/v1/campaigns/%d/pause", campaignID), nil, apiKey))
	if w.Code != http.StatusOK {
		t.Fatalf("pause want 200, got %d body=%s", w.Code, w.Body.String())
	}
	if !waitPaused(2 * time.Second) {
		t.Fatal("did not receive paused")
	}
}

func TestCampaign_Pause_NotActive_400(t *testing.T) {
	d := mustDeps(t)
	advID, apiKey := newAdvertiser(t, d)
	campaignID := newCampaign(t, d, advID) // draft, never started

	w := execPublic(t, d, authedReq(t, "POST", fmt.Sprintf("/api/v1/campaigns/%d/pause", campaignID), nil, apiKey))
	if w.Code != http.StatusBadRequest && w.Code != http.StatusConflict {
		t.Fatalf("pause draft: want 400/409, got %d body=%s", w.Code, w.Body.String())
	}
}

func TestCampaign_Get_NotFound_404(t *testing.T) {
	d := mustDeps(t)
	_, apiKey := newAdvertiser(t, d)
	w := execPublic(t, d, authedReq(t, "GET", "/api/v1/campaigns/99999999", nil, apiKey))
	if w.Code != http.StatusNotFound {
		t.Fatalf("want 404, got %d", w.Code)
	}
}
```

- [ ] **Step 2: 在 `e2e_support_test.go` 加 action-specific subscribe**

在 `subscribeUpdates` 下方加:

```go
// subscribeUpdatesAction returns a wait function that only fires for (campaignID, action).
func subscribeUpdatesAction(t *testing.T, rdb *redis.Client, campaignID int64, action string) func(time.Duration) bool {
	t.Helper()
	ctx := context.Background()
	pubsub := rdb.Subscribe(ctx, "campaign:updates")
	if _, err := pubsub.Receive(ctx); err != nil {
		t.Fatalf("subscribe confirm: %v", err)
	}
	got := make(chan struct{}, 1)
	go func() {
		for msg := range pubsub.Channel() {
			var p struct {
				CampaignID int64  `json:"campaign_id"`
				Action     string `json:"action"`
			}
			if err := json.Unmarshal([]byte(msg.Payload), &p); err != nil {
				continue
			}
			if p.CampaignID == campaignID && p.Action == action {
				select {
				case got <- struct{}{}:
				default:
				}
				return
			}
		}
	}()
	t.Cleanup(func() { _ = pubsub.Close() })
	return func(d time.Duration) bool {
		select {
		case <-got:
			return true
		case <-time.After(d):
			return false
		}
	}
}
```

- [ ] **Step 3: 跑测试**

Run: `go test -tags=e2e -count=1 -run TestCampaign ./internal/handler/...`
Expected: 全绿。

发现失败按 TDD 流程修:**不允许**修改测试迁就坏行为;修 handler。每个 bug 记入报告。

- [ ] **Step 4: Commit**

```bash
git add internal/handler/e2e_public_campaign_test.go internal/handler/e2e_support_test.go
git commit -m "test(handler): add e2e coverage for campaign CRUD + start/pause

Happy path + pub/sub assertions for update/start/pause actions, plus
pause-from-draft and 404 negative cases.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P2.5: Creative + meta 覆盖

**Files:**
- Create: `internal/handler/e2e_public_creative_test.go`
- Create: `internal/handler/e2e_public_meta_test.go`

**覆盖**:`GET /api/v1/campaigns/{id}/creatives`、`GET /api/v1/ad-types`、`GET /api/v1/billing-models`、`POST /api/v1/upload`、`GET /api/v1/audit-log`。

Creative CRUD 本身 (POST/PUT/DELETE) 的 happy-path 由 P1 的 pub/sub 测试已经覆盖了(201/200/200),这里只补 **错误分支**。

- [ ] **Step 1: 写 `e2e_public_creative_test.go`**

```go
//go:build e2e
// +build e2e

package handler_test

import (
	"fmt"
	"net/http"
	"testing"
)

func TestCreative_ListByCampaign(t *testing.T) {
	d := mustDeps(t)
	advID, apiKey := newAdvertiser(t, d)
	campaignID := newCampaign(t, d, advID)
	_ = newCreative(t, d, campaignID)

	w := execPublic(t, d, authedReq(t, "GET", fmt.Sprintf("/api/v1/campaigns/%d/creatives", campaignID), nil, apiKey))
	if w.Code != http.StatusOK {
		t.Fatalf("want 200, got %d", w.Code)
	}
}

func TestCreative_Create_BadAdType_400(t *testing.T) {
	d := mustDeps(t)
	advID, apiKey := newAdvertiser(t, d)
	campaignID := newCampaign(t, d, advID)

	body := map[string]any{
		"campaign_id":     campaignID,
		"name":            "bad",
		"ad_type":         "NOT_A_REAL_TYPE",
		"format":          "image",
		"size":            "300x250",
		"ad_markup":       "<a>ad</a>",
		"destination_url": "https://example.com",
	}
	w := execPublic(t, d, authedReq(t, "POST", "/api/v1/creatives", body, apiKey))
	if w.Code != http.StatusBadRequest {
		t.Fatalf("want 400, got %d body=%s", w.Code, w.Body.String())
	}
}

func TestCreative_Update_NotFound_404(t *testing.T) {
	d := mustDeps(t)
	_, apiKey := newAdvertiser(t, d)
	body := map[string]any{
		"name":            "x",
		"ad_type":         "banner",
		"format":          "image",
		"size":            "300x250",
		"ad_markup":       "<a>ad</a>",
		"destination_url": "https://example.com",
	}
	w := execPublic(t, d, authedReq(t, "PUT", "/api/v1/creatives/999999999", body, apiKey))
	if w.Code != http.StatusNotFound {
		t.Fatalf("want 404, got %d body=%s", w.Code, w.Body.String())
	}
}

func TestCreative_Delete_NotFound_404(t *testing.T) {
	d := mustDeps(t)
	_, apiKey := newAdvertiser(t, d)
	w := execPublic(t, d, authedReq(t, "DELETE", "/api/v1/creatives/999999999", nil, apiKey))
	if w.Code != http.StatusNotFound {
		t.Fatalf("want 404, got %d", w.Code)
	}
}
```

- [ ] **Step 2: 写 `e2e_public_meta_test.go`**

```go
//go:build e2e
// +build e2e

package handler_test

import (
	"bytes"
	"mime/multipart"
	"net/http"
	"net/http/httptest"
	"testing"
)

func TestMeta_AdTypes(t *testing.T) {
	d := mustDeps(t)
	w := execPublic(t, d, authedReq(t, "GET", "/api/v1/ad-types", nil, ""))
	if w.Code != http.StatusOK {
		t.Fatalf("want 200, got %d", w.Code)
	}
	body := w.Body.String()
	for _, want := range []string{"banner", "native", "interstitial", "splash"} {
		if !contains(body, want) {
			t.Fatalf("expected %q in ad-types body: %s", want, body)
		}
	}
}

func TestMeta_BillingModels(t *testing.T) {
	d := mustDeps(t)
	w := execPublic(t, d, authedReq(t, "GET", "/api/v1/billing-models", nil, ""))
	if w.Code != http.StatusOK {
		t.Fatalf("want 200, got %d", w.Code)
	}
	if !contains(w.Body.String(), "cpm") {
		t.Fatalf("expected 'cpm' in billing-models body: %s", w.Body.String())
	}
}

func TestMeta_AuditLog(t *testing.T) {
	d := mustDeps(t)
	_, apiKey := newAdvertiser(t, d)
	w := execPublic(t, d, authedReq(t, "GET", "/api/v1/audit-log", nil, apiKey))
	if w.Code != http.StatusOK {
		t.Fatalf("want 200, got %d", w.Code)
	}
}

func TestUpload_SmallPNG(t *testing.T) {
	d := mustDeps(t)
	_, apiKey := newAdvertiser(t, d)

	var buf bytes.Buffer
	mw := multipart.NewWriter(&buf)
	fw, _ := mw.CreateFormFile("file", "tiny.png")
	// smallest valid PNG (1x1 transparent)
	png := []byte{
		0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a,
		0x00, 0x00, 0x00, 0x0d, 0x49, 0x48, 0x44, 0x52,
		0x00, 0x00, 0x00, 0x01, 0x00, 0x00, 0x00, 0x01,
		0x08, 0x06, 0x00, 0x00, 0x00, 0x1f, 0x15, 0xc4,
		0x89, 0x00, 0x00, 0x00, 0x0a, 0x49, 0x44, 0x41,
		0x54, 0x78, 0x9c, 0x63, 0x00, 0x01, 0x00, 0x00,
		0x05, 0x00, 0x01, 0x0d, 0x0a, 0x2d, 0xb4, 0x00,
		0x00, 0x00, 0x00, 0x49, 0x45, 0x4e, 0x44, 0xae,
		0x42, 0x60, 0x82,
	}
	fw.Write(png)
	mw.Close()

	req := httptest.NewRequest("POST", "/api/v1/upload", &buf)
	req.Header.Set("Content-Type", mw.FormDataContentType())
	req.Header.Set("X-API-Key", apiKey)
	w := execPublic(t, d, req)
	if w.Code != http.StatusOK && w.Code != http.StatusCreated {
		t.Fatalf("want 200/201, got %d body=%s", w.Code, w.Body.String())
	}
	if !contains(w.Body.String(), "url") {
		t.Fatalf("expected url in response: %s", w.Body.String())
	}
}

func TestUpload_RejectNonImage(t *testing.T) {
	d := mustDeps(t)
	_, apiKey := newAdvertiser(t, d)

	var buf bytes.Buffer
	mw := multipart.NewWriter(&buf)
	fw, _ := mw.CreateFormFile("file", "evil.exe")
	fw.Write([]byte("MZ\x90\x00this is not an image"))
	mw.Close()

	req := httptest.NewRequest("POST", "/api/v1/upload", &buf)
	req.Header.Set("Content-Type", mw.FormDataContentType())
	req.Header.Set("X-API-Key", apiKey)
	w := execPublic(t, d, req)
	if w.Code != http.StatusBadRequest && w.Code != http.StatusUnsupportedMediaType {
		t.Fatalf("want 400/415, got %d body=%s", w.Code, w.Body.String())
	}
}
```

- [ ] **Step 3: 跑两个文件**

Run: `go test -tags=e2e -count=1 -run 'TestCreative|TestMeta|TestUpload' ./internal/handler/...`
Expected: 全绿。

- [ ] **Step 4: Commit**

```bash
git add internal/handler/e2e_public_creative_test.go internal/handler/e2e_public_meta_test.go
git commit -m "test(handler): add e2e coverage for creative list + meta + upload

Creative CRUD happy path already covered by P1 pub/sub tests; this adds
error branches (404, 400 bad ad_type) plus list endpoint, ad-types,
billing-models, audit-log, and upload (small PNG / reject non-image).

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P2.6: Reports + export + analytics 覆盖(含 ClickHouse fixture)

**Files:**
- Create: `internal/handler/e2e_public_report_test.go`

**覆盖**:所有 `/api/v1/reports/campaign/{id}/*` 路由 + `/api/v1/reports/overview` + `/api/v1/export/*` + `/api/v1/analytics/{stream,snapshot}`。

**策略**:一个测试建 advertiser + campaign,用 `insertBidLog` 注入 bid/win/impression/click/conversion 各 3 行,再打所有 reports endpoint 验证数字 > 0 或结构正确。

- [ ] **Step 1: 写测试文件**

```go
//go:build e2e
// +build e2e

package handler_test

import (
	"fmt"
	"net/http"
	"strings"
	"testing"
)

func TestReports_AllEndpoints(t *testing.T) {
	d := mustDeps(t)
	if d.ReportStore == nil {
		t.Skip("clickhouse not available")
	}
	advID, apiKey := newAdvertiser(t, d)
	campaignID := newCampaign(t, d, advID)
	creativeID := newCreative(t, d, campaignID)

	// Seed bid_log: 3 of each event type
	conn := mustCHConn(t)
	for _, et := range []string{"bid", "win", "impression", "click", "conversion"} {
		insertBidLog(t, conn, advID, campaignID, creativeID, et, 3)
	}

	endpoints := []string{
		fmt.Sprintf("/api/v1/reports/campaign/%d/stats", campaignID),
		fmt.Sprintf("/api/v1/reports/campaign/%d/hourly", campaignID),
		fmt.Sprintf("/api/v1/reports/campaign/%d/geo", campaignID),
		fmt.Sprintf("/api/v1/reports/campaign/%d/bids", campaignID),
		fmt.Sprintf("/api/v1/reports/campaign/%d/attribution", campaignID),
		fmt.Sprintf("/api/v1/reports/campaign/%d/simulate", campaignID),
		"/api/v1/reports/overview",
	}
	for _, path := range endpoints {
		t.Run(path, func(t *testing.T) {
			w := execPublic(t, d, authedReq(t, "GET", path, nil, apiKey))
			if w.Code != http.StatusOK {
				t.Fatalf("want 200, got %d body=%s", w.Code, w.Body.String())
			}
		})
	}
}

func TestReports_Export_CSV(t *testing.T) {
	d := mustDeps(t)
	if d.ReportStore == nil {
		t.Skip("clickhouse not available")
	}
	advID, apiKey := newAdvertiser(t, d)
	campaignID := newCampaign(t, d, advID)
	creativeID := newCreative(t, d, campaignID)
	conn := mustCHConn(t)
	insertBidLog(t, conn, advID, campaignID, creativeID, "impression", 5)

	for _, path := range []string{
		fmt.Sprintf("/api/v1/export/campaign/%d/stats", campaignID),
		fmt.Sprintf("/api/v1/export/campaign/%d/bids", campaignID),
	} {
		t.Run(path, func(t *testing.T) {
			w := execPublic(t, d, authedReq(t, "GET", path, nil, apiKey))
			if w.Code != http.StatusOK {
				t.Fatalf("want 200, got %d", w.Code)
			}
			ct := w.Header().Get("Content-Type")
			if !strings.Contains(ct, "csv") {
				t.Fatalf("expected csv content-type, got %q", ct)
			}
			if !strings.Contains(w.Body.String(), ",") {
				t.Fatalf("expected csv body with commas, got: %s", w.Body.String())
			}
		})
	}
}

func TestReports_Stats_ForbiddenOtherAdvertiser(t *testing.T) {
	d := mustDeps(t)
	advA, _ := newAdvertiser(t, d)
	_, keyB := newAdvertiser(t, d)
	campA := newCampaign(t, d, advA)

	w := execPublic(t, d, authedReq(t, "GET", fmt.Sprintf("/api/v1/reports/campaign/%d/stats", campA), nil, keyB))
	// Expect 403 or 404 (either is acceptable as "you can't see that")
	if w.Code != http.StatusForbidden && w.Code != http.StatusNotFound {
		t.Fatalf("want 403/404 for cross-advertiser access, got %d", w.Code)
	}
}

func TestAnalytics_Snapshot(t *testing.T) {
	d := mustDeps(t)
	_, apiKey := newAdvertiser(t, d)
	w := execPublic(t, d, authedReq(t, "GET", "/api/v1/analytics/snapshot", nil, apiKey))
	if w.Code != http.StatusOK {
		t.Fatalf("want 200, got %d", w.Code)
	}
}

func TestAnalytics_Stream_ContentType(t *testing.T) {
	d := mustDeps(t)
	_, apiKey := newAdvertiser(t, d)
	// SSE: we just verify the handler returns 200 and text/event-stream
	// without holding the connection open. httptest records without blocking.
	w := execPublic(t, d, authedReq(t, "GET", "/api/v1/analytics/stream", nil, apiKey))
	if w.Code != http.StatusOK {
		t.Fatalf("want 200, got %d", w.Code)
	}
	ct := w.Header().Get("Content-Type")
	if !strings.Contains(ct, "event-stream") {
		t.Fatalf("expected text/event-stream, got %q", ct)
	}
}
```

**注意**:报表 403/404 的精确性依赖 handler 实现。如果当前 handler 让别人读到别的广告主的 campaign,这就是 bug — **不要把测试改成 200 去迁就**,修 handler。

- [ ] **Step 2: 跑测试**

Run: `go test -tags=e2e -count=1 -run 'TestReports|TestAnalytics' ./internal/handler/...`
Expected: 全绿(或发现的问题按 TDD 修复)。

- [ ] **Step 3: Commit**

```bash
git add internal/handler/e2e_public_report_test.go
git commit -m "test(handler): add e2e coverage for reports + export + analytics

Seeds bid_log with 15 synthetic events (3 of each event type) and hits
all campaign report endpoints, overview, CSV export, and analytics
snapshot + stream. Includes cross-advertiser access negative test.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P2.7: Admin registration + creative review 覆盖

**Files:**
- Create: `internal/handler/e2e_admin_registration_test.go`
- Create: `internal/handler/e2e_admin_creative_test.go`

- [ ] **Step 1: 写 registration 测试**

```go
//go:build e2e
// +build e2e

package handler_test

import (
	"context"
	"fmt"
	"net/http"
	"testing"
)

func TestAdmin_InviteCodes_CreateAndList(t *testing.T) {
	d := mustDeps(t)
	body := map[string]any{"created_by": "qa-" + t.Name(), "max_uses": 10}
	w := execAdmin(t, d, adminReq(t, "POST", "/api/v1/admin/invite-codes", body))
	if w.Code != http.StatusCreated && w.Code != http.StatusOK {
		t.Fatalf("create invite: want 200/201, got %d body=%s", w.Code, w.Body.String())
	}
	var created struct {
		Code string `json:"code"`
	}
	decodeJSON(t, w, &created)
	if created.Code == "" {
		t.Fatal("missing code in response")
	}

	wList := execAdmin(t, d, adminReq(t, "GET", "/api/v1/admin/invite-codes", nil))
	if wList.Code != http.StatusOK {
		t.Fatalf("list invite: want 200, got %d", wList.Code)
	}
	if !contains(wList.Body.String(), created.Code) {
		t.Fatalf("expected to see new code %s in list: %s", created.Code, wList.Body.String())
	}
}

func TestAdmin_Registrations_ApproveFlow(t *testing.T) {
	d := mustDeps(t)
	pool := mustPool(t)
	code, err := d.RegSvc.CreateInviteCode(context.Background(), "qa-"+t.Name(), 10, nil)
	if err != nil {
		t.Fatalf("create invite: %v", err)
	}
	email := fmt.Sprintf("pending-%d@test.local", nowNano())
	// submit a registration through the public endpoint so status=pending
	_ = execPublic(t, d, authedReq(t, "POST", "/api/v1/register", map[string]any{
		"company_name":  "reg-" + t.Name(),
		"contact_email": email,
		"invite_code":   code,
	}, ""))

	// list pending
	w := execAdmin(t, d, adminReq(t, "GET", "/api/v1/admin/registrations?status=pending", nil))
	if w.Code != http.StatusOK {
		t.Fatalf("list: want 200, got %d", w.Code)
	}
	if !contains(w.Body.String(), email) {
		t.Fatalf("pending reg not in list: %s", w.Body.String())
	}

	// Look up the pending registration id directly via pg so we don't parse the body.
	// Table name is registration_requests per migrations/009_registration.sql; adjust
	// if that turns out to differ when this task runs (the pool query will fail with
	// a clear error if so).
	var pendingID int64
	if err := pool.QueryRow(context.Background(),
		`SELECT id FROM registration_requests WHERE contact_email = $1`, email).Scan(&pendingID); err != nil {
		t.Fatalf("lookup pending id (verify table name in migrations): %v", err)
	}

	// approve
	wA := execAdmin(t, d, adminReq(t, "POST", fmt.Sprintf("/api/v1/admin/registrations/%d/approve", pendingID), nil))
	if wA.Code != http.StatusOK {
		t.Fatalf("approve: want 200, got %d body=%s", wA.Code, wA.Body.String())
	}
}

func TestAdmin_Registrations_NoToken_401(t *testing.T) {
	d := mustDeps(t)
	// bypass the helper and hit execAdminWithAuth (which runs the middleware)
	req := adminReq(t, "GET", "/api/v1/admin/registrations", nil)
	req.Header.Del("X-Admin-Token")
	w := execAdminWithAuth(t, d, req)
	if w.Code != http.StatusUnauthorized {
		t.Fatalf("want 401, got %d", w.Code)
	}
}
```

**前置 assumption**:`registration.Service` 有 `DB()` 或等价方法暴露 `*pgxpool.Pool` / `pgx` 连接。如果没有,改用 `mustDeps(t)` 里已经连的 pool(需要在 `*handler.Deps` 或 helper 里暴露 db)。**执行到这一 task 时现场确认,如果需要在 `registration` 包加小 getter,单独加并说明 "仅测试用"**。

- [ ] **Step 2: 写 creative review 测试**

```go
//go:build e2e
// +build e2e

package handler_test

import (
	"context"
	"fmt"
	"net/http"
	"testing"
)

func TestAdmin_Creatives_ListPending(t *testing.T) {
	d := mustDeps(t)
	advID, _ := newAdvertiser(t, d)
	campaignID := newCampaign(t, d, advID)
	creativeID := newCreative(t, d, campaignID)
	// Force status=pending (newCreative uses Store.CreateCreative which defaults to pending)
	_ = d.Store.UpdateCreativeStatus(context.Background(), creativeID, "pending")

	w := execAdmin(t, d, adminReq(t, "GET", "/api/v1/admin/creatives?status=pending", nil))
	if w.Code != http.StatusOK {
		t.Fatalf("want 200, got %d", w.Code)
	}
	if !contains(w.Body.String(), fmt.Sprintf(`"id":%d`, creativeID)) {
		t.Fatalf("new pending creative %d not in list: %s", creativeID, w.Body.String())
	}
}

func TestAdmin_Creatives_Approve(t *testing.T) {
	d := mustDeps(t)
	advID, _ := newAdvertiser(t, d)
	campaignID := newCampaign(t, d, advID)
	creativeID := newCreative(t, d, campaignID)
	_ = d.Store.UpdateCreativeStatus(context.Background(), creativeID, "pending")

	w := execAdmin(t, d, adminReq(t, "POST", fmt.Sprintf("/api/v1/admin/creatives/%d/approve", creativeID), nil))
	if w.Code != http.StatusOK {
		t.Fatalf("approve: want 200, got %d body=%s", w.Code, w.Body.String())
	}

	// verify
	after, err := d.Store.GetCreativeByID(context.Background(), creativeID)
	if err != nil {
		t.Fatalf("get creative: %v", err)
	}
	if after.Status != "approved" {
		t.Fatalf("status want approved, got %s", after.Status)
	}
}

func TestAdmin_Creatives_Reject(t *testing.T) {
	d := mustDeps(t)
	advID, _ := newAdvertiser(t, d)
	campaignID := newCampaign(t, d, advID)
	creativeID := newCreative(t, d, campaignID)
	_ = d.Store.UpdateCreativeStatus(context.Background(), creativeID, "pending")

	body := map[string]any{"reason": "qa test reject"}
	w := execAdmin(t, d, adminReq(t, "POST", fmt.Sprintf("/api/v1/admin/creatives/%d/reject", creativeID), body))
	if w.Code != http.StatusOK {
		t.Fatalf("reject: want 200, got %d body=%s", w.Code, w.Body.String())
	}

	after, _ := d.Store.GetCreativeByID(context.Background(), creativeID)
	if after.Status != "rejected" {
		t.Fatalf("status want rejected, got %s", after.Status)
	}
}

func TestAdmin_Creatives_Approve_NotFound_404(t *testing.T) {
	d := mustDeps(t)
	w := execAdmin(t, d, adminReq(t, "POST", "/api/v1/admin/creatives/999999999/approve", nil))
	if w.Code != http.StatusNotFound {
		t.Fatalf("want 404, got %d body=%s", w.Code, w.Body.String())
	}
}
```

- [ ] **Step 3: 跑测试**

Run: `go test -tags=e2e -count=1 -run 'TestAdmin_InviteCodes|TestAdmin_Registrations|TestAdmin_Creatives' ./internal/handler/...`
Expected: 全绿。

- [ ] **Step 4: Commit**

```bash
git add internal/handler/e2e_admin_registration_test.go internal/handler/e2e_admin_creative_test.go
git commit -m "test(handler): add e2e coverage for admin registration + creative review

Invite code CRUD, registration approval flow, creative approve/reject
state machine transitions, plus 401 no-admin-token and 404 not-found
negative cases.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P2.8: Admin system(health + circuit + advertisers + topup + active-campaigns)

**Files:**
- Create: `internal/handler/e2e_admin_system_test.go`

- [ ] **Step 1: 写测试文件**

```go
//go:build e2e
// +build e2e

package handler_test

import (
	"fmt"
	"net/http"
	"testing"
)

func TestAdmin_Health(t *testing.T) {
	d := mustDeps(t)
	w := execAdmin(t, d, adminReq(t, "GET", "/api/v1/admin/health", nil))
	if w.Code != http.StatusOK {
		t.Fatalf("want 200, got %d", w.Code)
	}
}

func TestAdmin_CircuitBreakAndReset(t *testing.T) {
	d := mustDeps(t)
	if d.Guardrail == nil {
		t.Skip("guardrail not configured")
	}
	// break
	w := execAdmin(t, d, adminReq(t, "POST", "/api/v1/admin/circuit-break", nil))
	if w.Code != http.StatusOK {
		t.Fatalf("break: want 200, got %d", w.Code)
	}
	// status
	wS := execAdmin(t, d, adminReq(t, "GET", "/api/v1/admin/circuit-status", nil))
	if wS.Code != http.StatusOK {
		t.Fatalf("status: want 200, got %d", wS.Code)
	}
	// reset (cleanup)
	wR := execAdmin(t, d, adminReq(t, "POST", "/api/v1/admin/circuit-reset", nil))
	if wR.Code != http.StatusOK {
		t.Fatalf("reset: want 200, got %d", wR.Code)
	}
}

func TestAdmin_ListAdvertisers_IncludesMine(t *testing.T) {
	d := mustDeps(t)
	advID, _ := newAdvertiser(t, d)

	w := execAdmin(t, d, adminReq(t, "GET", "/api/v1/admin/advertisers", nil))
	if w.Code != http.StatusOK {
		t.Fatalf("want 200, got %d", w.Code)
	}
	if !contains(w.Body.String(), fmt.Sprintf(`"id":%d`, advID)) {
		t.Fatalf("new advertiser %d not in list", advID)
	}
}

func TestAdmin_TopUp_UpdatesBalance(t *testing.T) {
	d := mustDeps(t)
	advID, _ := newAdvertiser(t, d)

	body := map[string]any{"advertiser_id": advID, "amount_cents": 12345}
	w := execAdmin(t, d, adminReq(t, "POST", "/api/v1/admin/topup", body))
	if w.Code != http.StatusOK && w.Code != http.StatusCreated {
		t.Fatalf("want 200/201, got %d body=%s", w.Code, w.Body.String())
	}
}

func TestAdmin_ActiveCampaigns(t *testing.T) {
	d := mustDeps(t)
	w := execAdmin(t, d, adminReq(t, "GET", "/internal/active-campaigns", nil))
	if w.Code != http.StatusOK {
		t.Fatalf("want 200, got %d", w.Code)
	}
}

func TestAdmin_AuditLog(t *testing.T) {
	d := mustDeps(t)
	w := execAdmin(t, d, adminReq(t, "GET", "/api/v1/admin/audit-log", nil))
	if w.Code != http.StatusOK {
		t.Fatalf("want 200, got %d", w.Code)
	}
}
```

- [ ] **Step 2: 跑测试**

Run: `go test -tags=e2e -count=1 -run TestAdmin_ ./internal/handler/...`
Expected: 全绿。

- [ ] **Step 3: Commit**

```bash
git add internal/handler/e2e_admin_system_test.go
git commit -m "test(handler): add e2e coverage for admin system endpoints

Health, circuit breaker (break/reset/status), advertiser list, admin
topup, active-campaigns, and audit log.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P2.9: 通用 authz table-driven 测试

**Files:**
- Create: `internal/handler/e2e_authz_table_test.go`

**目的**:所有 public 路由一键验 401,所有 admin 路由一键验 401。不和具体 handler 逻辑耦合。

- [ ] **Step 1: 写表驱动测试**

```go
//go:build e2e
// +build e2e

package handler_test

import (
	"context"
	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/heartgryphon/dsp/internal/handler"
)

func TestAuthz_PublicRoutes_401WithoutAPIKey(t *testing.T) {
	d := mustDeps(t)
	mux := handler.BuildPublicMux(d)
	// Real auth middleware in front
	lookup := func(ctx context.Context, key string) (int64, string, string, error) {
		adv, err := d.Store.GetAdvertiserByAPIKey(ctx, key)
		if err != nil {
			return 0, "", "", err
		}
		return adv.ID, adv.CompanyName, adv.ContactEmail, nil
	}
	withAuth := authMiddlewareImpl(lookup, mux)
	withExemption := handler.WithAuthExemption(withAuth, mux)

	cases := []struct {
		method string
		path   string
	}{
		{"GET", "/api/v1/campaigns"},
		{"POST", "/api/v1/campaigns"},
		{"GET", "/api/v1/campaigns/1"},
		{"PUT", "/api/v1/campaigns/1"},
		{"POST", "/api/v1/campaigns/1/start"},
		{"POST", "/api/v1/campaigns/1/pause"},
		{"POST", "/api/v1/creatives"},
		{"PUT", "/api/v1/creatives/1"},
		{"DELETE", "/api/v1/creatives/1"},
		{"GET", "/api/v1/reports/campaign/1/stats"},
		{"GET", "/api/v1/reports/overview"},
		{"POST", "/api/v1/billing/topup"},
		{"GET", "/api/v1/billing/transactions"},
		{"GET", "/api/v1/billing/balance/1"},
		{"GET", "/api/v1/audit-log"},
		{"GET", "/api/v1/analytics/snapshot"},
		{"GET", "/api/v1/analytics/stream"},
		{"POST", "/api/v1/upload"},
	}
	// These paths are exempt from auth (register/health/docs/ad-types/billing-models/uploads/)
	// and must NOT appear above.
	for _, c := range cases {
		t.Run(c.method+" "+c.path, func(t *testing.T) {
			req := httptest.NewRequest(c.method, c.path, nil)
			// no X-API-Key
			w := httptest.NewRecorder()
			withExemption.ServeHTTP(w, req)
			if w.Code != http.StatusUnauthorized {
				t.Fatalf("want 401, got %d body=%s", w.Code, w.Body.String())
			}
		})
	}
}

func TestAuthz_AdminRoutes_401WithoutToken(t *testing.T) {
	d := mustDeps(t)
	mux := handler.BuildAdminMux(d)
	withAuth := handler.AdminAuthMiddleware(mux)

	cases := []struct {
		method string
		path   string
	}{
		{"GET", "/internal/active-campaigns"},
		{"GET", "/api/v1/admin/registrations"},
		{"POST", "/api/v1/admin/registrations/1/approve"},
		{"POST", "/api/v1/admin/registrations/1/reject"},
		{"GET", "/api/v1/admin/health"},
		{"GET", "/api/v1/admin/creatives"},
		{"POST", "/api/v1/admin/creatives/1/approve"},
		{"POST", "/api/v1/admin/creatives/1/reject"},
		{"POST", "/api/v1/admin/circuit-break"},
		{"POST", "/api/v1/admin/circuit-reset"},
		{"GET", "/api/v1/admin/circuit-status"},
		{"GET", "/api/v1/admin/advertisers"},
		{"POST", "/api/v1/admin/topup"},
		{"POST", "/api/v1/admin/invite-codes"},
		{"GET", "/api/v1/admin/invite-codes"},
		{"GET", "/api/v1/admin/audit-log"},
	}
	for _, c := range cases {
		t.Run(c.method+" "+c.path, func(t *testing.T) {
			req := httptest.NewRequest(c.method, c.path, nil)
			w := httptest.NewRecorder()
			withAuth.ServeHTTP(w, req)
			if w.Code != http.StatusUnauthorized {
				t.Fatalf("want 401, got %d body=%s", w.Code, w.Body.String())
			}
		})
	}
}
```

- [ ] **Step 2: 跑表驱动**

Run: `go test -tags=e2e -count=1 -run TestAuthz ./internal/handler/...`
Expected: 全部 case 都 401。

**可能发现的 bug**:某些路由走 `WithAuthExemption` 实际上没放行。修路由或修中间件,把 fix 记进报告。

- [ ] **Step 3: Commit**

```bash
git add internal/handler/e2e_authz_table_test.go
git commit -m "test(handler): add table-driven authz coverage for all routes

All non-exempt public routes must return 401 without X-API-Key;
all admin routes must return 401 without X-Admin-Token.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P2.10: P2 verification loop

**Files:** 无代码。执行 CLAUDE.md 完整循环,最多 5 轮。

- [ ] **Step 1: Round 1 全量 e2e**

```bash
docker compose up -d
go test -tags=e2e -count=1 ./internal/handler/... 2>&1 | tee /tmp/p2-round1-e2e.log
go test ./... 2>&1 | tee /tmp/p2-round1-unit.log
```

Expected: 两条日志都无 FAIL。

- [ ] **Step 2: Round 1 — requesting-code-review**

Invoke `superpowers:requesting-code-review` on P2 的累计 diff(`git diff becdc67..HEAD -- internal/handler cmd/api internal/campaign`)。焦点:
- 测试是否真的在验证**语义**(而不是 200 通过就算绿)
- 跨测试数据隔离(唯一邮箱)
- pub/sub 订阅时序(subscribe before publish)
- `BuildPublicMux` / `BuildAdminMux` 是否遗漏了任何 `main.go` 原本有的路由
- `BuildPublicHandler` 中间件顺序与原 `main.go` 一致

修完 Critical/Important 回到 Step 1。

- [ ] **Step 3: Round 1 — 前端 /qa smoke**

P2 没碰前端。做一次 smoke:
```
/qa 扫首页 + /campaigns + /campaigns/new,看是否被 P2 的 routes.go 重构 break 了(应该 0 破坏)
```

- [ ] **Step 4: 判定进 P3**

任何轮次有修复就从 Step 1 重跑。整轮零问题才进 P3。

更新报告:

```markdown
## Phase 2 · 后端 Go e2e 测试基建 + 全 handler 覆盖

- routes.go 抽取 commit: <hash>
- e2e 测试总数: N
- 跑 N 轮 verification loop
- 发现并修复的 bug:
  - B002 · ...
  - B003 · ...
```

---

# Phase P3 · 脚本 harness + 后端 bug triage

**目标**:`scripts/qa/run.sh` 端到端跑完整业务流,发现 bug 按阻塞/非阻塞分档,阻塞的走 TDD 修复流程(补 Go 测试 → 改 handler → 重跑 harness)。

**Exit 标准**:harness 从零到底一次通过,所有阻塞 fix 都有 Go 测试兜底,verification loop 零问题一轮。

---

### Task P3.1: Bootstrap + env

**Files:**
- Create: `scripts/qa/e2e-env.sh`
- Create: `scripts/qa/lib.sh`
- Create: `scripts/qa/00-bootstrap.sh`
- Create: `scripts/qa/run.sh`

- [ ] **Step 1: 写 `e2e-env.sh`**

```bash
#!/usr/bin/env bash
# scripts/qa/e2e-env.sh
# Source this before running any other script in this directory.
export DSP_E2E_PG_DSN="${DSP_E2E_PG_DSN:-postgres://dsp:dsp@localhost:16432/dsp?sslmode=disable}"
export DSP_E2E_REDIS_ADDR="${DSP_E2E_REDIS_ADDR:-localhost:17380}"
export DSP_E2E_CH_ADDR="${DSP_E2E_CH_ADDR:-localhost:19124}"
export DSP_E2E_CH_HTTP="${DSP_E2E_CH_HTTP:-http://localhost:20001}"
export DSP_E2E_ADMIN_TOKEN="${DSP_E2E_ADMIN_TOKEN:-admin-secret}"
export DSP_E2E_API="${DSP_E2E_API:-http://localhost:19181}"
export DSP_E2E_ADMIN_API="${DSP_E2E_ADMIN_API:-http://localhost:19182}"
```

- [ ] **Step 2: 写 `lib.sh`**

```bash
#!/usr/bin/env bash
# scripts/qa/lib.sh
# Common helpers for harness scripts.
set -euo pipefail

QA_STATE_DIR="${QA_STATE_DIR:-/tmp/biz-qa-$$}"
mkdir -p "$QA_STATE_DIR"

log() { printf '[%s] %s\n' "$(date +%H:%M:%S)" "$*" >&2; }
fail() { log "FAIL: $*"; exit 1; }

# curl_json METHOD PATH [BODY_JSON] [API_KEY]
curl_json() {
  local method="$1"; shift
  local path="$1"; shift
  local body="${1:-}"; [ "$#" -gt 0 ] && shift || true
  local key="${1:-}"
  local args=(-sS -X "$method" -w '\n%{http_code}')
  [ -n "$key" ] && args+=(-H "X-API-Key: $key")
  args+=(-H "Content-Type: application/json")
  if [ -n "$body" ]; then
    args+=(--data "$body")
  fi
  curl "${args[@]}" "${DSP_E2E_API}${path}"
}

# curl_admin METHOD PATH [BODY_JSON]
curl_admin() {
  local method="$1"; shift
  local path="$1"; shift
  local body="${1:-}"
  local args=(-sS -X "$method" -w '\n%{http_code}' -H "X-Admin-Token: ${DSP_E2E_ADMIN_TOKEN}" -H "Content-Type: application/json")
  if [ -n "$body" ]; then
    args+=(--data "$body")
  fi
  curl "${args[@]}" "${DSP_E2E_ADMIN_API}${path}"
}

# assert_status EXPECTED RESPONSE_WITH_STATUS
# RESPONSE_WITH_STATUS comes from curl_json/curl_admin (body + newline + status code)
assert_status() {
  local expected="$1"
  local resp="$2"
  local status="${resp##*$'\n'}"
  local body="${resp%$'\n'*}"
  if [ "$status" != "$expected" ]; then
    log "expected $expected, got $status"
    log "body: $body"
    return 1
  fi
  printf '%s' "$body"
}

json_field() {
  local body="$1"
  local field="$2"
  printf '%s' "$body" | jq -r ".$field"
}

save_state() {
  printf '%s' "$2" > "$QA_STATE_DIR/$1"
}

load_state() {
  cat "$QA_STATE_DIR/$1"
}

step_ok() { log "[ok] $*"; }
step_start() { log "---- $* ----"; }
```

- [ ] **Step 3: 写 `00-bootstrap.sh`**

```bash
#!/usr/bin/env bash
# scripts/qa/00-bootstrap.sh
# Wait for docker stack to be ready.
set -euo pipefail
source "$(dirname "$0")/lib.sh"

step_start "bootstrap"

max_wait=60
elapsed=0
while [ "$elapsed" -lt "$max_wait" ]; do
  if curl -sfo /dev/null "${DSP_E2E_API}/health"; then
    step_ok "public api ${DSP_E2E_API}/health"
    break
  fi
  sleep 1
  elapsed=$((elapsed + 1))
done
[ "$elapsed" -ge "$max_wait" ] && fail "public api /health timed out after ${max_wait}s — run: docker compose up -d"

while ! curl -sfo /dev/null "${DSP_E2E_ADMIN_API}/health"; do
  sleep 1
done
step_ok "admin api ${DSP_E2E_ADMIN_API}/health"

# Check optional tools
for tool in jq curl; do
  command -v "$tool" >/dev/null || fail "missing tool: $tool"
done
if ! command -v redis-cli >/dev/null; then
  log "warn: redis-cli not on host; pub/sub assertions will use docker exec fallback"
fi
if ! command -v clickhouse-client >/dev/null; then
  log "warn: clickhouse-client not on host; bid_log seed will use HTTP"
fi

step_ok "bootstrap complete"
```

- [ ] **Step 4: 写 `run.sh`**

```bash
#!/usr/bin/env bash
# scripts/qa/run.sh
# Main harness entry.
set -euo pipefail
cd "$(dirname "$0")"
source ./e2e-env.sh
source ./lib.sh

REPORT="docs/qa/2026-04-14-biz-qa-report.md"
cd ../..  # back to repo root

mkdir -p "$(dirname "$REPORT")"

log "== biz QA harness start =="
log "state: $QA_STATE_DIR"

STEPS=(
  00-bootstrap.sh
  10-invite-admin.sh
  20-register.sh
  30-topup.sh
  40-campaign.sh
  50-creative.sh
  60-reports.sh
  70-admin-review.sh
  99-teardown.sh
)

failed_steps=()
for step in "${STEPS[@]}"; do
  log ">> $step"
  if ! bash "scripts/qa/$step"; then
    log "!! $step FAILED"
    failed_steps+=("$step")
    break  # fail-fast
  fi
done

if [ "${#failed_steps[@]}" -eq 0 ]; then
  log "== ALL PASSED =="
  exit 0
else
  log "== FAILED: ${failed_steps[*]} =="
  exit 1
fi
```

- [ ] **Step 5: 让脚本可执行并跑 bootstrap**

```bash
chmod +x scripts/qa/*.sh
bash scripts/qa/00-bootstrap.sh
```
Expected: `bootstrap complete`。

- [ ] **Step 6: Commit**

```bash
git add scripts/qa/e2e-env.sh scripts/qa/lib.sh scripts/qa/00-bootstrap.sh scripts/qa/run.sh
git commit -m "test(qa): add harness bootstrap + lib + entry script

Skeleton for the biz QA scripted harness. 00-bootstrap waits for
docker stack health, lib.sh provides curl/admin/assert helpers, and
run.sh drives the per-step scripts added in follow-up commits.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P3.2: Invite code + register + approve 步骤

**Files:**
- Create: `scripts/qa/10-invite-admin.sh`
- Create: `scripts/qa/20-register.sh`

- [ ] **Step 1: 写 `10-invite-admin.sh`**

```bash
#!/usr/bin/env bash
# scripts/qa/10-invite-admin.sh
# Admin creates an invite code.
set -euo pipefail
source "$(dirname "$0")/lib.sh"
source "$(dirname "$0")/e2e-env.sh"

step_start "10-invite-admin"

body='{"note":"qa harness"}'
resp=$(curl_admin POST /api/v1/admin/invite-codes "$body")
ok=$(assert_status 201 "$resp" 2>/dev/null) || ok=$(assert_status 200 "$resp") || fail "create invite code"
code=$(printf '%s' "$ok" | jq -r '.code')
[ -z "$code" ] || [ "$code" = "null" ] && fail "missing code in response: $ok"

save_state invite_code "$code"
step_ok "invite code = $code"
```

- [ ] **Step 2: 写 `20-register.sh`**

```bash
#!/usr/bin/env bash
# scripts/qa/20-register.sh
# Advertiser registers with invite, admin approves, we retrieve the API key.
set -euo pipefail
source "$(dirname "$0")/lib.sh"
source "$(dirname "$0")/e2e-env.sh"

step_start "20-register"

invite=$(load_state invite_code)
ts=$(date +%s)
email="qa-harness-${ts}@test.local"
company="QA Harness ${ts}"

body=$(jq -n --arg e "$email" --arg c "$company" --arg i "$invite" \
  '{company_name:$c, contact_email:$e, invite_code:$i}')

resp=$(curl_json POST /api/v1/register "$body")
ok=$(assert_status 201 "$resp" 2>/dev/null) || ok=$(assert_status 200 "$resp") || fail "register"

# find the pending registration via admin list
list_resp=$(curl_admin GET "/api/v1/admin/registrations?status=pending")
list_body=$(assert_status 200 "$list_resp") || fail "list pending"
reg_id=$(printf '%s' "$list_body" | jq -r --arg e "$email" '.[] | select(.contact_email==$e) | .id')
[ -z "$reg_id" ] || [ "$reg_id" = "null" ] && fail "new registration not in pending list"

# approve
approve_resp=$(curl_admin POST "/api/v1/admin/registrations/${reg_id}/approve" "{}")
approve_body=$(assert_status 200 "$approve_resp") || fail "approve"

# Retrieve API key: the approve response should contain it, OR we list advertisers and match email.
api_key=$(printf '%s' "$approve_body" | jq -r '.api_key // empty')
if [ -z "$api_key" ]; then
  adv_resp=$(curl_admin GET /api/v1/admin/advertisers)
  adv_body=$(assert_status 200 "$adv_resp") || fail "list advertisers"
  api_key=$(printf '%s' "$adv_body" | jq -r --arg e "$email" '.[] | select(.contact_email==$e) | .api_key')
fi
[ -z "$api_key" ] || [ "$api_key" = "null" ] && fail "could not obtain api_key"

# Find advertiser_id
adv_id=$(curl_admin GET /api/v1/admin/advertisers | head -n -1 | jq -r --arg e "$email" '.[] | select(.contact_email==$e) | .id')
[ -z "$adv_id" ] && fail "could not find advertiser id"

save_state advertiser_id "$adv_id"
save_state api_key "$api_key"
save_state contact_email "$email"

step_ok "advertiser_id=$adv_id email=$email api_key=${api_key:0:16}…"
```

- [ ] **Step 3: 测跑两个步骤**

```bash
source scripts/qa/e2e-env.sh
bash scripts/qa/00-bootstrap.sh
bash scripts/qa/10-invite-admin.sh
bash scripts/qa/20-register.sh
```

Expected: 都打印 `[ok]`,`$QA_STATE_DIR` 里有 `invite_code` / `advertiser_id` / `api_key` / `contact_email` 四个文件。

**发现 bug 时**:记进 `docs/qa/2026-04-14-biz-qa-report.md` 的 B### 列表,按分档规则处理。

- [ ] **Step 4: Commit**

```bash
git add scripts/qa/10-invite-admin.sh scripts/qa/20-register.sh
git commit -m "test(qa): add harness steps for invite code + register + approve

Admin creates invite → advertiser registers → admin approves → harness
captures api_key and advertiser_id for subsequent steps.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P3.3: Topup + campaign + creative + reports + review 步骤

**Files:**
- Create: `scripts/qa/30-topup.sh`
- Create: `scripts/qa/40-campaign.sh`
- Create: `scripts/qa/50-creative.sh`
- Create: `scripts/qa/60-reports.sh`
- Create: `scripts/qa/70-admin-review.sh`
- Create: `scripts/qa/99-teardown.sh`

- [ ] **Step 1: `30-topup.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/lib.sh"
source "$(dirname "$0")/e2e-env.sh"
step_start "30-topup"

adv_id=$(load_state advertiser_id)
api_key=$(load_state api_key)

body=$(jq -n --argjson a "$adv_id" '{advertiser_id:$a, amount_cents:500000}')
resp=$(curl_json POST /api/v1/billing/topup "$body" "$api_key")
ok=$(assert_status 200 "$resp" 2>/dev/null) || ok=$(assert_status 201 "$resp") || fail "topup"

bal_resp=$(curl_json GET "/api/v1/billing/balance/${adv_id}" "" "$api_key")
bal=$(assert_status 200 "$bal_resp") || fail "balance"
cents=$(printf '%s' "$bal" | jq -r '.balance_cents')
[ "$cents" -lt 500000 ] && fail "balance too low: $cents"

step_ok "balance_cents=$cents"
```

- [ ] **Step 2: `40-campaign.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/lib.sh"
source "$(dirname "$0")/e2e-env.sh"
step_start "40-campaign"

adv_id=$(load_state advertiser_id)
api_key=$(load_state api_key)

body=$(jq -n --argjson a "$adv_id" '{
  advertiser_id: $a,
  name: "qa-harness-campaign",
  bid_cpm_cents: 150,
  daily_budget: 100000,
  billing_model: "cpm",
  targeting: {}
}')
resp=$(curl_json POST /api/v1/campaigns "$body" "$api_key")
ok=$(assert_status 201 "$resp") || fail "create campaign"
cid=$(printf '%s' "$ok" | jq -r '.id')
save_state campaign_id "$cid"
step_ok "campaign_id=$cid (draft)"

# Update
upd=$(jq -n '{name:"qa-harness-campaign-renamed", bid_cpm_cents:200, daily_budget:200000}')
resp=$(curl_json PUT "/api/v1/campaigns/${cid}" "$upd" "$api_key")
assert_status 200 "$resp" >/dev/null || fail "update campaign"
step_ok "campaign updated"

# Start (subscribe to campaign:updates first to verify activated event)
sub_log="$QA_STATE_DIR/updates-${cid}.log"
: > "$sub_log"
if command -v redis-cli >/dev/null; then
  (redis-cli -h "${DSP_E2E_REDIS_ADDR%:*}" -p "${DSP_E2E_REDIS_ADDR##*:}" SUBSCRIBE campaign:updates > "$sub_log" 2>&1) &
else
  (docker compose exec -T redis redis-cli SUBSCRIBE campaign:updates > "$sub_log" 2>&1) &
fi
sub_pid=$!
sleep 0.3

resp=$(curl_json POST "/api/v1/campaigns/${cid}/start" "" "$api_key")
assert_status 200 "$resp" >/dev/null || { kill $sub_pid 2>/dev/null; fail "start campaign"; }
sleep 0.7
kill $sub_pid 2>/dev/null || true
wait $sub_pid 2>/dev/null || true

grep -q "\"action\":\"activated\"" "$sub_log" || {
  log "sub log: $(cat "$sub_log")"
  fail "start did not publish activated"
}
step_ok "campaign started + activated event verified"
```

- [ ] **Step 3: `50-creative.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/lib.sh"
source "$(dirname "$0")/e2e-env.sh"
step_start "50-creative"

cid=$(load_state campaign_id)
api_key=$(load_state api_key)

# Subscribe for 'updated' on campaign:updates (create + update + delete should all publish)
sub_log="$QA_STATE_DIR/creative-updates.log"
: > "$sub_log"
if command -v redis-cli >/dev/null; then
  (redis-cli -h "${DSP_E2E_REDIS_ADDR%:*}" -p "${DSP_E2E_REDIS_ADDR##*:}" SUBSCRIBE campaign:updates > "$sub_log" 2>&1) &
else
  (docker compose exec -T redis redis-cli SUBSCRIBE campaign:updates > "$sub_log" 2>&1) &
fi
sub_pid=$!
sleep 0.3

# Create
body=$(jq -n --argjson c "$cid" '{
  campaign_id: $c,
  name: "qa-creative",
  ad_type: "banner",
  format: "image",
  size: "300x250",
  ad_markup: "<a>ad</a>",
  destination_url: "https://example.com"
}')
resp=$(curl_json POST /api/v1/creatives "$body" "$api_key")
ok=$(assert_status 201 "$resp") || { kill $sub_pid 2>/dev/null; fail "create creative"; }
creative_id=$(printf '%s' "$ok" | jq -r '.id')
save_state creative_id "$creative_id"

# Update
upd=$(jq -n '{
  name:"qa-creative-v2",
  ad_type:"banner",
  format:"image",
  size:"300x250",
  ad_markup:"<a>new</a>",
  destination_url:"https://example.com/v2"
}')
resp=$(curl_json PUT "/api/v1/creatives/${creative_id}" "$upd" "$api_key")
assert_status 200 "$resp" >/dev/null || { kill $sub_pid 2>/dev/null; fail "update creative"; }

# Delete
resp=$(curl_json DELETE "/api/v1/creatives/${creative_id}" "" "$api_key")
assert_status 200 "$resp" >/dev/null || { kill $sub_pid 2>/dev/null; fail "delete creative"; }

sleep 0.7
kill $sub_pid 2>/dev/null || true
wait $sub_pid 2>/dev/null || true

# Expect 3 'updated' messages for our campaign_id
got=$(grep -c "\"campaign_id\":${cid}" "$sub_log" || true)
[ "$got" -lt 3 ] && { log "only got $got updates (want ≥3): $(cat "$sub_log")"; fail "creative CRUD did not publish 3× updated"; }
step_ok "creative CRUD published 3+ campaign:updates"
```

- [ ] **Step 4: `60-reports.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/lib.sh"
source "$(dirname "$0")/e2e-env.sh"
step_start "60-reports"

adv_id=$(load_state advertiser_id)
api_key=$(load_state api_key)
cid=$(load_state campaign_id)

# Seed bid_log: 5 impressions via ClickHouse HTTP (no clickhouse-client dependency)
seed_sql=$(cat <<SQL
INSERT INTO bid_log (event_date, event_time, campaign_id, creative_id, advertiser_id, exchange_id, request_id, geo_country, device_os, device_id, bid_price_cents, clear_price_cents, charge_cents, event_type, loss_reason)
VALUES
(today(), now(), ${cid}, 0, ${adv_id}, 'qa-exchange', 'qa-req-1', 'US', 'iOS', 'd1', 100, 80, 80, 4, ''),
(today(), now(), ${cid}, 0, ${adv_id}, 'qa-exchange', 'qa-req-2', 'US', 'iOS', 'd2', 100, 80, 80, 4, ''),
(today(), now(), ${cid}, 0, ${adv_id}, 'qa-exchange', 'qa-req-3', 'CN', 'Android', 'd3', 100, 80, 80, 5, ''),
(today(), now(), ${cid}, 0, ${adv_id}, 'qa-exchange', 'qa-req-4', 'US', 'iOS', 'd4', 100, 80, 80, 6, ''),
(today(), now(), ${cid}, 0, ${adv_id}, 'qa-exchange', 'qa-req-5', 'US', 'iOS', 'd5', 100, 80, 80, 2, '')
SQL
)
curl -sS -X POST --data-binary "$seed_sql" "${DSP_E2E_CH_HTTP}/?database=default" >/dev/null || fail "clickhouse seed"
step_ok "bid_log seeded"

for path in \
  "/api/v1/reports/campaign/${cid}/stats" \
  "/api/v1/reports/campaign/${cid}/hourly" \
  "/api/v1/reports/campaign/${cid}/geo" \
  "/api/v1/reports/campaign/${cid}/bids" \
  "/api/v1/reports/campaign/${cid}/attribution" \
  "/api/v1/reports/overview"
do
  resp=$(curl_json GET "$path" "" "$api_key")
  assert_status 200 "$resp" >/dev/null || fail "GET $path"
  step_ok "$path"
done

# Export CSV
for path in \
  "/api/v1/export/campaign/${cid}/stats" \
  "/api/v1/export/campaign/${cid}/bids"
do
  resp=$(curl_json GET "$path" "" "$api_key")
  assert_status 200 "$resp" >/dev/null || fail "CSV $path"
  step_ok "$path"
done
```

- [ ] **Step 5: `70-admin-review.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/lib.sh"
source "$(dirname "$0")/e2e-env.sh"
step_start "70-admin-review"

# List pending creatives
resp=$(curl_admin GET "/api/v1/admin/creatives?status=pending")
body=$(assert_status 200 "$resp") || fail "list pending"
step_ok "pending list retrieved"

# If there are any, approve the first one (not strictly required since 50 deleted our creative)
pending_id=$(printf '%s' "$body" | jq -r '.[0].id // empty')
if [ -n "$pending_id" ] && [ "$pending_id" != "null" ]; then
  resp=$(curl_admin POST "/api/v1/admin/creatives/${pending_id}/approve" "{}")
  assert_status 200 "$resp" >/dev/null || fail "approve creative"
  step_ok "approved creative $pending_id"
else
  step_ok "no pending creatives to approve (expected if harness 50 deleted its creative)"
fi
```

- [ ] **Step 6: `99-teardown.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/lib.sh"
source "$(dirname "$0")/e2e-env.sh"
step_start "99-teardown"

adv_id=$(load_state advertiser_id 2>/dev/null || echo '?')
cid=$(load_state campaign_id 2>/dev/null || echo '?')
email=$(load_state contact_email 2>/dev/null || echo '?')

log "summary:"
log "  advertiser_id=$adv_id email=$email"
log "  campaign_id=$cid"
log "  state dir: $QA_STATE_DIR (not cleaned)"
step_ok "harness complete"
```

- [ ] **Step 7: 跑整个 harness**

```bash
chmod +x scripts/qa/*.sh
bash scripts/qa/run.sh 2>&1 | tee /tmp/p3-harness.log
```

Expected: `== ALL PASSED ==`。

**如果失败**:
1. 看日志定位哪个 step 挂
2. 判断是 **harness bug**(脚本写错)还是 **handler bug**(API 行为错误)
3. 脚本 bug → 修脚本
4. handler bug → 记进报告,按 TDD 走:
   - 在 `internal/handler/e2e_*_test.go` 加一条精准复现的 Go 测试(红)
   - 修 handler(绿)
   - 重跑 harness

- [ ] **Step 8: Commit(harness 全部 pass 后)**

```bash
git add scripts/qa/30-topup.sh scripts/qa/40-campaign.sh scripts/qa/50-creative.sh \
         scripts/qa/60-reports.sh scripts/qa/70-admin-review.sh scripts/qa/99-teardown.sh
git commit -m "test(qa): add harness steps for topup through admin review

Full advertiser flow: topup + balance check, campaign create/update/start
with pub/sub verification, creative CRUD with 3× campaign:updates
assertion, reports (with bid_log seed via ClickHouse HTTP), CSV export,
and admin review. Fail-fast with trap on any assertion miss.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P3.4: Harness 产出初版 QA 报告

**Files:**
- Create: `docs/qa/2026-04-14-biz-qa-report.md`

- [ ] **Step 1: 写初版报告**

```markdown
# biz QA 报告 — 2026-04-14

## 栈版本
- git commit: <git rev-parse HEAD>
- branch: biz
- docker compose services: up/healthy

## Phase 1 · Creative pub/sub 修复
- 3 条路由 (POST/PUT/DELETE /api/v1/creatives) 接入 campaign:updates
- Go e2e 测试: TestCreativePubSub_{Create,Update,Delete}
- verification loop 轮数: 1

## Phase 2 · 后端 Go e2e 基建 + 覆盖
- routes.go 抽取 + e2e helpers + N 个测试文件
- Go e2e 测试总数: <N>
- 发现 + 修复的 bug:
  - (none so far)

## Phase 3 · 脚本 harness
- scripts/qa/*.sh 完整跑过
- 发现 + 修复的 bug:
  - (填入 harness 发现的每个 bug,按 B### 编号)

## 发现的 bug 列表

### B001 · [status] <short description>
- 路由:
- 复现:
- 根因:
- 修复 commit:
- 回归测试:

## 未覆盖(明确)
- engine 侧(cmd/bidder, cmd/consumer, internal/bidder, internal/events)
- dsp.billing topic / SendLoss(契约 §3.1/3.2)
- 性能 / 压测

## 契约文档待变更(本轮不改,留给合回 main)
- (填入)
```

替换 `<...>` 占位:
- `<git rev-parse HEAD>`:运行 `git rev-parse HEAD` 粘贴
- `<N>`:运行 `ls internal/handler/e2e_*_test.go | wc -l` 粘贴

- [ ] **Step 2: Commit 报告初版**

```bash
git add docs/qa/2026-04-14-biz-qa-report.md
git commit -m "docs(qa): add biz QA round report (initial)

Will be updated through P4 (frontend) and finalized in P5 with
screenshots and the contract change list.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P3.5: P3 verification loop

- [ ] **Step 1: Round 1 — 跑完整 harness**

```bash
bash scripts/qa/run.sh
```
Expected: ALL PASSED。

- [ ] **Step 2: Round 1 — Go e2e 全跑**

```bash
go test -tags=e2e -count=1 ./internal/handler/...
```
Expected: 全绿(P3 修过的 handler bug 应已有 Go 测试兜底)。

- [ ] **Step 3: Round 1 — requesting-code-review**

Review P3 累计的 diff(脚本 + 任何 handler 修复)。焦点:harness 脚本的 `set -e` 是否到位、pub/sub 订阅时序、fail-fast 路径。

修完任何问题回 Step 1 重跑下一轮。整轮零问题进 P4。

- [ ] **Step 4: 更新报告**

在 `docs/qa/2026-04-14-biz-qa-report.md` Phase 3 章节填入:
- harness 跑了几轮
- 发现的所有 bug(B 号 + 状态)
- 修复 commit

---

# Phase P4 · 前端 /qa + DESIGN.md 合规 + 契约漂移

**目标**:`web/app/**` 全量页面按 CLAUDE.md 四维度 + 契约漂移 扫描,发现前端 bug 就地修,后端漂移按 TDD 修。

**Exit 标准**:所有页面四维度 + 契约漂移通过,DESIGN.md 合规差异为 0 或有理由记录,verification loop 零问题一轮。

本 Phase 的 task 是**审计驱动**的,代码产出取决于发现。每个 audit task 给出:要测的内容、判定标准、修复规则。

---

### Task P4.1: 读取 DESIGN.md + web 文档 + OpenAPI regenerate 流程

**Files:** 无代码产出,信息落入报告。

- [ ] **Step 1: 读文档并抄权威值到报告**

Read:
- `DESIGN.md`(权威字体/颜色/间距/断点)
- `web/AGENTS.md`(Next.js 非标准版本提醒)
- `web/CLAUDE.md`
- `web/README.md`(如存在)

把 DESIGN.md 的权威值抄到 `docs/qa/2026-04-14-biz-qa-report.md` 新增"合规基准"章节:

```markdown
## P4 合规基准(从 DESIGN.md 抄录)

- 字体: <...>
- 主色: <...>
- 间距 base: <...>
- 断点: <...>
```

如 DESIGN.md 某项缺失,写 "MISSING — 本轮按现有页面平均值兜底"。

- [ ] **Step 2: 查 OpenAPI regenerate 流程**

Run:
```bash
grep -rni "openapi\|swagger" Makefile 2>/dev/null || echo "(no Makefile target)"
ls scripts/ 2>/dev/null | grep -i openapi || echo "(no script)"
```

把发现写入报告 "契约 regenerate 流程" 章节。如果没有任何规范流程,记"未找到,本轮不引入新工具,漂移只记不修"。

- [ ] **Step 3: 启动 web dev server**

```bash
cd web && npm run dev
```
后台跑,记端口(应是 15000,看 `package.json` 的脚本和 `next.config.ts`)。

- [ ] **Step 4: Commit 报告更新**

```bash
git add docs/qa/2026-04-14-biz-qa-report.md
git commit -m "docs(qa): add P4 design baseline + openapi regen status

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P4.2: 审计首页 `/`(含未登录态)

**Files:** 按发现修改 `web/app/page.tsx` / `web/app/_components/ApiKeyGate.tsx` 等。

- [ ] **Step 1: /qa 扫未登录态**

打开浏览器到 `http://localhost:15000/`,**清空 localStorage 的 `dsp_api_key`**(重点:首页必须能在无 api key 时渲染登录 UI,不能白屏)。

执行 CLAUDE.md 四维度:
- 直觉检查:有没有布局异常、重复组件、缺失元素?
- 交互:是否有"输入 api key → 保存 → 跳转"的可用路径?错误 key 是否报错?
- 视觉:至少 3 个元素(登录卡片/输入框/按钮)CSS 抽查对齐 DESIGN.md
- 数据:N/A(未登录态无数据)

记录每一条不合规到 P4 报告章节。

- [ ] **Step 2: /qa 扫已登录态**

用有效 api key 登录(可以直接 `localStorage.setItem('dsp_api_key', '<key>')` + reload),在首页执行四维度。

- [ ] **Step 3: 修前端 bug**

对每条发现的前端 bug:
1. 识别根因文件(`page.tsx` / `globals.css` / `_components/*`)
2. 改代码
3. 热重载验证
4. 再跑 /qa 对应维度确认

**改前必须阅读 `web/AGENTS.md` 提示的 `node_modules/next/dist/docs/` 相关 guide**,避免用过时 API。

- [ ] **Step 4: 契约漂移扫描(仅针对首页用到的 endpoint)**

查看 `web/app/page.tsx` + `ApiKeyGate.tsx` 用到的 API 调用,对比:
- `cmd/api/main.go` 的路由声明
- `docs/generated/openapi3.yaml` 的字段
- `web/lib/api-types.ts` 的类型

记录漂移到报告。阻塞性(前端实际读的字段后端没返)且有 regen 流程就 regen;否则只记录。

- [ ] **Step 5: Commit(如有修改)**

```bash
git add web/app/page.tsx web/app/_components/ApiKeyGate.tsx docs/qa/2026-04-14-biz-qa-report.md
git commit -m "fix(web): <specific fixes> to landing page

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P4.3: 审计 `/campaigns` 列表页

**Files:** 按发现修改 `web/app/campaigns/page.tsx`。

- [ ] **Step 1: /qa 四维度 + 契约漂移**

- 直觉:列表、空状态、loading 是否都合理?
- 交互:点击某行是否进详情页?返回是否保留滚动位置?新建按钮是否跳 `/campaigns/new`?
- 视觉:行内按钮 / 状态 badge / 表头 CSS 抽查
- 数据:页面列表数 vs `SELECT COUNT(*) FROM campaigns WHERE advertiser_id = <登录广告主 id>`(用 `psql -h localhost -p 16432 ...` 打)
- 契约:`lib/api.ts` 里 `listCampaigns` 的调用路径、响应字段 vs `cmd/api/main.go:128` 的 `HandleListCampaigns` 返回

- [ ] **Step 2: 修 + commit(同 P4.2 流程)**

---

### Task P4.4: 审计 `/campaigns/new` + `/campaigns/[id]`

**Files:** `web/app/campaigns/new/page.tsx`, `web/app/campaigns/[id]/page.tsx`。

- [ ] **Step 1: `/campaigns/new` 四维度**

- 交互:表单每个必填字段空/合法/非法;提交按钮禁用逻辑;成功后跳详情页
- 数据:N/A(只写入)
- 契约:POST 请求体 vs `HandleCreateCampaign` 期望字段

- [ ] **Step 2: `/campaigns/[id]` 四维度**

- 交互:编辑字段→保存;启动/暂停按钮(状态机按钮应随 status 变灰/可点);创意 CRUD 区;返回列表
- 视觉:详情页卡片 / tab / 按钮组 CSS 抽查
- 数据:页面显示的字段 vs `SELECT * FROM campaigns WHERE id = ?`(含 targeting JSON 展示)
- 契约:GET 响应字段、PUT 请求字段、POST start/pause 响应 vs 后端

- [ ] **Step 3: 修 + commit**

---

### Task P4.5: 审计 `/reports` + `/reports/[id]` + `/analytics`

**Files:** `web/app/reports/page.tsx`, `web/app/reports/[id]/page.tsx`, `web/app/analytics/page.tsx`。

- [ ] **Step 1: `/reports` 四维度**

- 交互:从列表选一个 campaign 进 `/reports/[id]`
- 数据:N/A(只是入口)

- [ ] **Step 2: `/reports/[id]` 四维度**

- 交互:时间范围切换、图表切 tab
- 视觉:图表容器、图例、数字卡片 CSS 抽查
- 数据:页面数字 vs ClickHouse `bid_log` 聚合(用 `curl http://localhost:20001/ -d "SELECT count() FROM bid_log WHERE campaign_id = ? AND event_type = 4"`)
- 契约:`stats` / `hourly` / `geo` / `bids` / `attribution` / `simulate` 每个 endpoint 的响应字段 vs 前端 type

- [ ] **Step 3: `/analytics` 四维度**

- 交互:SSE 是否连上并实时更新?断开后是否重连?
- 视觉:实时图表 CSS 抽查
- 数据:snapshot 显示的数字 vs ClickHouse 查询

- [ ] **Step 4: 修 + commit**

---

### Task P4.6: 审计 `/billing` 页

**Files:** `web/app/billing/page.tsx`。

- [ ] **Step 1: 四维度**

- 交互:充值输入金额→提交;交易记录分页
- 视觉:余额卡片、交易表 CSS 抽查
- 数据:余额数字 vs `/api/v1/billing/balance/{id}`;交易列表 vs `advertiser_balance_transactions` 表
- 契约:topup 请求体 / balance 响应 / transactions 响应 vs 后端

- [ ] **Step 2: 修 + commit**

---

### Task P4.7: 审计 `/admin`(首页 + invites + agencies + creatives + audit)

**Files:** `web/app/admin/page.tsx`, `web/app/admin/invites/page.tsx`, `web/app/admin/agencies/page.tsx`, `web/app/admin/creatives/page.tsx`, `web/app/admin/audit/page.tsx`。

**注意**:admin 页面需要 `X-Admin-Token`,前端要先在登录态里拿到或在页面顶部要求输入。测试前确认本地 admin 页能认证。

- [ ] **Step 1: 每个子页面走四维度 + 契约漂移**

按如下 checklist:

| 页面 | 关键交互 | 数据对齐 | 契约对齐 |
|---|---|---|---|
| `/admin` | 导航到子页面 | — | — |
| `/admin/invites` | 建 code → 列表可见;点过期/复制 | list vs `SELECT * FROM invite_codes` | POST/GET `/api/v1/admin/invite-codes` |
| `/admin/agencies` | 列表 + 审核通过/拒绝 | list vs `SELECT * FROM registration_requests WHERE status=?` | GET/POST `/api/v1/admin/registrations/*` |
| `/admin/creatives` | 列表 + approve/reject(带原因) | list vs `SELECT * FROM creatives WHERE status='pending'` | GET/POST `/api/v1/admin/creatives/*` |
| `/admin/audit` | 列表 + 过滤 | list vs `SELECT * FROM audit_log LIMIT 50` | GET `/api/v1/admin/audit-log` |

- [ ] **Step 2: 修 + commit**

---

### Task P4.8: P4 verification loop

- [ ] **Step 1: Round 1 — requesting-code-review**

Review P4 累计的前端 diff。焦点:
- Next.js API 用法是否符合 `web/AGENTS.md` 提醒(非标准版本)
- API 调用是否都走 `web/lib/api.ts`(不要在 page.tsx 里硬编码 URL)
- 错误/空/loading 状态覆盖
- 跨页面状态(登录态、权限)是否一致

- [ ] **Step 2: Round 1 — verification(启动栈 + 前端)**

```bash
docker compose up -d
# web dev server 在后台
go test -tags=e2e -count=1 ./internal/handler/...  # 确保 P4 的后端修改没破坏 e2e
bash scripts/qa/run.sh  # 确保 P4 的后端修改没破坏 harness
```

- [ ] **Step 3: Round 1 — /qa 全量**

```
/qa 扫所有 P4 任务涉及的页面,四维度 + 契约漂移
```

- [ ] **Step 4: 判定进 P5**

任何修就从 Step 1 重跑。整轮零问题进 P5。上限 5 轮。

---

# Phase P5 · 最终 review + /browse 截图 + 合并

**目标**:final-code-review 全量、截图存证、报告终版、合回 main。

---

### Task P5.1: Final code review

- [ ] **Step 1: Review 本轮累计 diff**

```bash
git log --oneline becdc67..HEAD
git diff becdc67..HEAD --stat
```

Invoke `superpowers:requesting-code-review` 给整轮 diff 做全量审查。

- [ ] **Step 2: 修 Critical / Important**

发现的问题就地修,每个修复单独 commit。

---

### Task P5.2: /browse 截图关键页面

**Files:**
- Create: `docs/qa/screenshots/2026-04-14/*.png`

- [ ] **Step 1: 三档响应式截图**

对每个关键页面(首页登录态、campaigns 列表、campaign 详情、reports 详情、analytics、billing、admin 每个子页)跑 /browse,三档断点(1440 / 768 / 375)各截一张,文件名格式 `<page>-<breakpoint>.png`,存 `docs/qa/screenshots/2026-04-14/`。

- [ ] **Step 2: 关键交互 before/after 截图**

- 广告启动前/后(draft/active badge 变化)
- 充值前/后(balance 数字变化)
- 创意创建前/后(列表变化)

存到同一目录,文件名 `<feature>-<before|after>.png`。

- [ ] **Step 3: Commit 截图**

```bash
git add docs/qa/screenshots/2026-04-14/
git commit -m "docs(qa): add browse screenshots for 2026-04-14 biz QA round

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P5.3: 报告终版 + 契约变更清单

**Files:**
- Modify: `docs/qa/2026-04-14-biz-qa-report.md`

- [ ] **Step 1: 填完报告所有章节**

把 Phase 1-4 的实际数据、bug 列表、覆盖数字、未解决事项、截图引用全部补齐。

- [ ] **Step 2: 加"契约变更清单"章节**

在报告末尾加:

```markdown
## 契约文档变更清单(本轮禁止改,待合回 main 时在 main 上统一改)

### docs/contracts/biz-engine.md §1 "已知缺口"
本轮已补齐 POST/PUT/DELETE /api/v1/creatives 的 campaign:updates 发布。
合回 main 后,需要把以下三条从 §1 "已知缺口" 小节移除,并在 §1 主表格
"发布点(权威清单)" 追加三行:

| POST /api/v1/creatives | HandleCreateCreative | updated | 新增创意,刷 bidder 缓存 |
| PUT /api/v1/creatives/{id} | HandleUpdateCreative | updated | 编辑创意属性 |
| DELETE /api/v1/creatives/{id} | HandleDeleteCreative | updated | 删除创意,清 bidder 缓存 |

### 其他漂移
(本轮发现的所有其他契约漂移点,每条记路径 + 漂移内容 + 建议修改)
```

- [ ] **Step 3: Commit**

```bash
git add docs/qa/2026-04-14-biz-qa-report.md
git commit -m "docs(qa): finalize biz QA round report with contract change list

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

---

### Task P5.4: P5 verification loop + 合回 main

- [ ] **Step 1: Round 1 — 全量测试**

```bash
docker compose up -d
go test ./...                                              # 单测
go test -tags=e2e -count=1 ./internal/handler/...          # e2e
bash scripts/qa/run.sh                                      # harness
```
三条都要绿。

- [ ] **Step 2: Round 1 — /qa 全量 smoke**

快速扫所有关键页面,确认 P5 的任何 review 修复没回退。

- [ ] **Step 3: Round 1 — /browse 终验**

对比报告的截图和当前页面,确认一致。

任何修就从 Step 1 重跑。整轮零问题进 Step 4。

- [ ] **Step 4: 合回 main**

Invoke `superpowers:finishing-a-development-branch` skill。选择 PR 或直接 merge 根据 skill 指引。

在 PR 描述里引用:
- 本计划:`docs/superpowers/plans/2026-04-14-biz-qa.md`
- 设计 spec:`docs/superpowers/specs/2026-04-14-biz-qa-design.md`
- QA 报告:`docs/qa/2026-04-14-biz-qa-report.md`
- 契约变更清单(见报告末尾)

---

## 附录:运行小抄

```bash
# 启动栈
docker compose up -d

# 跑所有 e2e
source scripts/qa/e2e-env.sh
go test -tags=e2e -count=1 ./internal/handler/...

# 跑 harness
bash scripts/qa/run.sh

# 查 pg
psql "postgres://dsp:dsp@localhost:16432/dsp"

# 查 clickhouse(HTTP)
curl "http://localhost:20001/?database=default" -d "SELECT count() FROM bid_log"

# 查 redis
redis-cli -h localhost -p 17380 ping

# 看 api 日志
docker compose logs -f api

# 重置(破坏性,跨 Phase 正常不要用)
docker compose down -v && docker compose up -d
```
